name: Build containers
# When environment.yml is changed
on:
  pull_request:
    paths:
      - "**/Dockerfile"
      - "**/environment.yml"

env:
  WAVE_VER: "1.0.0"

jobs:
  gen-matrix:
    name: generate-matrix
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Calculate file differences
        id: diff
        uses: tj-actions/changed-files@v35
        with:
          json: true
          quotepath: false
          # TODO Add Dockerfiles
          files: |
            modules/**/environment.yml
      - name: Debug
        run: echo ${{ steps.diff.outputs.all_changed_files }}
      # - id: set-matrix
      #   run: echo "matrix={\"files\":${{ steps.diff.outputs.all_changed_files }}}" >> "$GITHUB_OUTPUT"

    outputs:
      files: ${{ steps.diff.outputs.all_changed_files }}

  build:
    if: "${{ fromJson(needs.gen-matrix.outputs.files) }}"
    needs: gen-matrix
    name: build
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        files: "${{ fromJson(needs.gen-matrix.outputs.files) }}"
        profile: ["docker", "singularity"]
    steps:
      # Use the @nf-core-bot token to check out so we can push later
      - uses: actions/checkout@v3

      - name: Install wave-cli
        run: |
          wget -q https://github.com/seqeralabs/wave-cli/releases/download/v${WAVE_VER}/wave-${WAVE_VER}-linux-x86_64
          sudo mv wave-${WAVE_VER}-linux-x86_64 /usr/local/bin/wave
          chmod +x /usr/local/bin/wave

      - name: Build container
        run: |
          wave --conda-file "${{ matrix.files }}" \
              --freeze \
              --build-repo ghcr.io/nf-core/modules/bowtie \
              --tower-token ${{ secrets.TOWER_ACCESS_TOKEN }} \
              --tower-workspace-id ${{ secrets.TOWER_WORKSPACE_ID }} \
              ${[ "${{ matrix.profile }}" == "singularity" ]:+--singularity}

    # TODO Build from Dockerfiles
