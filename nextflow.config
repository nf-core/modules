process {
    withName: PHOLD_RUN {
        cpus = 8
        accelerator = 1
        memory = '10 GB'
        ext.prefix = 'test_output'
    }
}

profiles {
    conda {
        conda.enabled = true
    }
}