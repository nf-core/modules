nextflow_process {

    name "Test Process EIDO_CONVERT"
    script "modules/nf-core/eido/convert/main.nf"
    process "EIDO_CONVERT"
    config "tests/modules/nf-core/eido_convert/nextflow.config"
    tag "eido_convert"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
