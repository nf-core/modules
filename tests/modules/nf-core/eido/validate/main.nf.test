nextflow_process {

    name "Test Process EIDO_VALIDATE"
    script "modules/nf-core/eido/validate/main.nf"
    process "EIDO_VALIDATE"
    config "tests/modules/nf-core/eido_validate/nextflow.config"
    tag "eido_validate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
