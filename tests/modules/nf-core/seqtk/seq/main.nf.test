nextflow_process {

    name "Test Process SEQTK_SEQ"
    script "modules/nf-core/seqtk/seq/main.nf"
    process "SEQTK_SEQ"
    config "tests/modules/nf-core/seqtk_seq/nextflow.config"
    tag "seqtk_seq"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
