nextflow_process {

    name "Test Process KRAKENUNIQ_DOWNLOAD"
    script "modules/nf-core/krakenuniq/download/main.nf"
    process "KRAKENUNIQ_DOWNLOAD"
    config "tests/modules/nf-core/krakenuniq_download/nextflow.config"
    tag "krakenuniq_download"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
