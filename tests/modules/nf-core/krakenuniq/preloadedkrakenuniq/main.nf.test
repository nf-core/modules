nextflow_process {

    name "Test Process KRAKENUNIQ_PRELOADEDKRAKENUNIQ"
    script "modules/nf-core/krakenuniq/preloadedkrakenuniq/main.nf"
    process "KRAKENUNIQ_PRELOADEDKRAKENUNIQ"
    config "tests/modules/nf-core/krakenuniq_preloadedkrakenuniq/nextflow.config"
    tag "krakenuniq_preloadedkrakenuniq"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
