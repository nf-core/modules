nextflow_process {

    name "Test Process ROARY"
    script "modules/nf-core/roary/main.nf"
    process "ROARY"
    config "tests/modules/nf-core/roary/nextflow.config"
    tag "roary"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
