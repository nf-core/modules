nextflow_process {

    name "Test Process CLONALFRAMEML"
    script "modules/nf-core/clonalframeml/main.nf"
    process "CLONALFRAMEML"
    config "tests/modules/nf-core/clonalframeml/nextflow.config"
    tag "clonalframeml"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
