nextflow_process {

    name "Test Process GAPPA_EXAMINEHEATTREE"
    script "modules/nf-core/gappa/examineheattree/main.nf"
    process "GAPPA_EXAMINEHEATTREE"
    config "tests/modules/nf-core/gappa_examineheattree/nextflow.config"
    tag "gappa_examineheattree"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
