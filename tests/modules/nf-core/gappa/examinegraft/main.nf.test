nextflow_process {

    name "Test Process GAPPA_EXAMINEGRAFT"
    script "modules/nf-core/gappa/examinegraft/main.nf"
    process "GAPPA_EXAMINEGRAFT"
    config "tests/modules/nf-core/gappa_examinegraft/nextflow.config"
    tag "gappa_examinegraft"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
