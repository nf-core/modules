nextflow_process {

    name "Test Process FLYE"
    script "modules/nf-core/flye/main.nf"
    process "FLYE"
    config "tests/modules/nf-core/flye/nextflow.config"
    tag "flye"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
