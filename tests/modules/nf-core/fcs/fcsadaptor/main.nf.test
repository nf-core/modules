nextflow_process {

    name "Test Process FCS_FCSADAPTOR"
    script "modules/nf-core/fcs/fcsadaptor/main.nf"
    process "FCS_FCSADAPTOR"
    config "tests/modules/nf-core/fcs_fcsadaptor/nextflow.config"
    tag "fcs_fcsadaptor"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
