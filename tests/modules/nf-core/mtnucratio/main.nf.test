nextflow_process {

    name "Test Process MTNUCRATIO"
    script "modules/nf-core/mtnucratio/main.nf"
    process "MTNUCRATIO"
    config "tests/modules/nf-core/mtnucratio/nextflow.config"
    tag "mtnucratio"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
