nextflow_process {

    name "Test Process FLASH"
    script "modules/nf-core/flash/main.nf"
    process "FLASH"
    config "tests/modules/nf-core/flash/nextflow.config"
    tag "flash"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
