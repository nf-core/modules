nextflow_process {

    name "Test Process FQ_GENERATE"
    script "modules/nf-core/fq/generate/main.nf"
    process "FQ_GENERATE"
    config "tests/modules/nf-core/fq_generate/nextflow.config"
    tag "fq_generate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
