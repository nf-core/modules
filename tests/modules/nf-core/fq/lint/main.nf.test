nextflow_process {

    name "Test Process FQ_LINT"
    script "modules/nf-core/fq/lint/main.nf"
    process "FQ_LINT"
    config "tests/modules/nf-core/fq_lint/nextflow.config"
    tag "fq_lint"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
