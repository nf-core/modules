nextflow_process {

    name "Test Process MASH_SKETCH"
    script "modules/nf-core/mash/sketch/main.nf"
    process "MASH_SKETCH"
    config "tests/modules/nf-core/mash_sketch/nextflow.config"
    tag "mash_sketch"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
