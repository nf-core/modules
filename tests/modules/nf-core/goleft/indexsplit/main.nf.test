nextflow_process {

    name "Test Process GOLEFT_INDEXSPLIT"
    script "modules/nf-core/goleft/indexsplit/main.nf"
    process "GOLEFT_INDEXSPLIT"
    config "tests/modules/nf-core/goleft_indexsplit/nextflow.config"
    tag "goleft_indexsplit"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
