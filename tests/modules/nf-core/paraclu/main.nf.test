nextflow_process {

    name "Test Process PARACLU"
    script "modules/nf-core/paraclu/main.nf"
    process "PARACLU"
    config "tests/modules/nf-core/paraclu/nextflow.config"
    tag "paraclu"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
