nextflow_process {

    name "Test Process COREOGRAPH"
    script "modules/nf-core/coreograph/main.nf"
    process "COREOGRAPH"
    config "tests/modules/nf-core/coreograph/nextflow.config"
    tag "coreograph"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
