nextflow_process {

    name "Test Process SALSA2"
    script "modules/nf-core/salsa2/main.nf"
    process "SALSA2"
    config "tests/modules/nf-core/salsa2/nextflow.config"
    tag "salsa2"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
