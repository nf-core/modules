nextflow_process {

    name "Test Process BISCUIT_VCF2BED"
    script "modules/nf-core/biscuit/vcf2bed/main.nf"
    process "BISCUIT_VCF2BED"
    config "tests/modules/nf-core/biscuit_vcf2bed/nextflow.config"
    tag "biscuit_vcf2bed"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
