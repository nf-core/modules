nextflow_process {

    name "Test Process BISCUIT_INDEX"
    script "modules/nf-core/biscuit/index/main.nf"
    process "BISCUIT_INDEX"
    config "tests/modules/nf-core/biscuit_index/nextflow.config"
    tag "biscuit_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
