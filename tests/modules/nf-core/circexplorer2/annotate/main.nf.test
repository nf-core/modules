nextflow_process {

    name "Test Process CIRCEXPLORER2_ANNOTATE"
    script "modules/nf-core/circexplorer2/annotate/main.nf"
    process "CIRCEXPLORER2_ANNOTATE"
    config "tests/modules/nf-core/circexplorer2_annotate/nextflow.config"
    tag "circexplorer2_annotate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
