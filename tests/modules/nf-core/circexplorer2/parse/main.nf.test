nextflow_process {

    name "Test Process CIRCEXPLORER2_PARSE"
    script "modules/nf-core/circexplorer2/parse/main.nf"
    process "CIRCEXPLORER2_PARSE"
    config "tests/modules/nf-core/circexplorer2_parse/nextflow.config"
    tag "circexplorer2_parse"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
