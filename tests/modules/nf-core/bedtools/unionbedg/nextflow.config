process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }
    
     withName: BEDTOOLS_UNIONBEDG_EMPTY {
        ext.args = "-empty -g ${params.test_data['sarscov2']['genome']['test_bedgraph']}"
    }

}
