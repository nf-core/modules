nextflow_process {

    name "Test Process BEDTOOLS_COVERAGE"
    script "modules/nf-core/bedtools/coverage/main.nf"
    process "BEDTOOLS_COVERAGE"
    config "tests/modules/nf-core/bedtools_coverage/nextflow.config"
    tag "bedtools_coverage"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
