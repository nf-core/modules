nextflow_process {

    name "Test Process BEDTOOLS_GENOMECOV"
    script "modules/nf-core/bedtools/genomecov/main.nf"
    process "BEDTOOLS_GENOMECOV"
    config "tests/modules/nf-core/bedtools_genomecov/nextflow.config"
    tag "bedtools_genomecov"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
