nextflow_process {

    name "Test Process BEDTOOLS_CLOSEST"
    script "modules/nf-core/bedtools/closest/main.nf"
    process "BEDTOOLS_CLOSEST"
    config "tests/modules/nf-core/bedtools_closest/nextflow.config"
    tag "bedtools_closest"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
