nextflow_process {

    name "Test Process BEDTOOLS_SHIFT"
    script "modules/nf-core/bedtools/shift/main.nf"
    process "BEDTOOLS_SHIFT"
    config "tests/modules/nf-core/bedtools_shift/nextflow.config"
    tag "bedtools_shift"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
