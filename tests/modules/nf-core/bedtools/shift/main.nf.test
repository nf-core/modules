nextflow_process {

    name "Test Process BEDTOOLS_SHIFT"
    script "modules/nf-core/bedtools/shift/main.nf"
    process "BEDTOOLS_SHIFT"
    config "tests/modules/nf-core/bedtools/shift/nextflow.config"
    tag "bedtools_shift"

    test("Sarscov2 Genome BED") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = [ [ id:'test'],
                             file(params.test_data['sarscov2']['genome']['test_bed'], checkIfExists: true)
                           ]
                input[1] = [ [id:'sizes'],
                             file(params.test_data['sarscov2']['genome']['genome_sizes'], checkIfExists: true)
                           ]

                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }
    }
}
