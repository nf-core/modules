nextflow_process {

    name "Test Process BEDTOOLS_COMPLEMENT"
    script "modules/nf-core/bedtools/complement/main.nf"
    process "BEDTOOLS_COMPLEMENT"
    config "tests/modules/nf-core/bedtools_complement/nextflow.config"
    tag "bedtools_complement"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
