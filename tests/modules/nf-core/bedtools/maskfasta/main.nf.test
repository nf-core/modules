nextflow_process {

    name "Test Process BEDTOOLS_MASKFASTA"
    script "modules/nf-core/bedtools/maskfasta/main.nf"
    process "BEDTOOLS_MASKFASTA"
    config "tests/modules/nf-core/bedtools/maskfasta/nextflow.config"
    tag "bedtools_maskfasta"

    test("Sarscov2 Genome BED") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = [ [ id:'test'],
                            file(params.test_data['sarscov2']['genome']['test_bed'], checkIfExists: true)
                           ]
                input[1] = file(params.test_data['sarscov2']['genome']['genome_fasta'], checkIfExists: true)
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }
    }
}
