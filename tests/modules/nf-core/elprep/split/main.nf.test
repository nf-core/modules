nextflow_process {

    name "Test Process ELPREP_SPLIT"
    script "modules/nf-core/elprep/split/main.nf"
    process "ELPREP_SPLIT"
    config "tests/modules/nf-core/elprep_split/nextflow.config"
    tag "elprep_split"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
