nextflow_process {

    name "Test Process UNTARFILES"
    script "modules/nf-core/untarfiles/main.nf"
    process "UNTARFILES"
    config "tests/modules/nf-core/untarfiles/nextflow.config"
    tag "untarfiles"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
