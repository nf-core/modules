nextflow_process {

    name "Test Process SSUISSERO"
    script "modules/nf-core/ssuissero/main.nf"
    process "SSUISSERO"
    config "tests/modules/nf-core/ssuissero/nextflow.config"
    tag "ssuissero"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
