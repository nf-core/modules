nextflow_process {

    name "Test Process MACREL_CONTIGS"
    script "modules/nf-core/macrel/contigs/main.nf"
    process "MACREL_CONTIGS"
    config "tests/modules/nf-core/macrel_contigs/nextflow.config"
    tag "macrel_contigs"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
