nextflow_process {

    name "Test Process ISMAPPER"
    script "modules/nf-core/ismapper/main.nf"
    process "ISMAPPER"
    config "tests/modules/nf-core/ismapper/nextflow.config"
    tag "ismapper"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
