nextflow_process {

    name "Test Process GATK4_CONDENSEDEPTHEVIDENCE"
    script "modules/nf-core/gatk4/condensedepthevidence/main.nf"
    process "GATK4_CONDENSEDEPTHEVIDENCE"
    config "tests/modules/nf-core/gatk4_condensedepthevidence/nextflow.config"
    tag "gatk4_condensedepthevidence"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
