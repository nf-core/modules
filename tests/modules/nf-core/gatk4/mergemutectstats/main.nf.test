nextflow_process {

    name "Test Process GATK4_MERGEMUTECTSTATS"
    script "modules/nf-core/gatk4/mergemutectstats/main.nf"
    process "GATK4_MERGEMUTECTSTATS"
    config "tests/modules/nf-core/gatk4_mergemutectstats/nextflow.config"
    tag "gatk4_mergemutectstats"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
