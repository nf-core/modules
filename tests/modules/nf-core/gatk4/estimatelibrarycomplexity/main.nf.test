nextflow_process {

    name "Test Process GATK4_ESTIMATELIBRARYCOMPLEXITY"
    script "modules/nf-core/gatk4/estimatelibrarycomplexity/main.nf"
    process "GATK4_ESTIMATELIBRARYCOMPLEXITY"
    config "tests/modules/nf-core/gatk4_estimatelibrarycomplexity/nextflow.config"
    tag "gatk4_estimatelibrarycomplexity"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
