nextflow_process {

    name "Test Process GATK4_APPLYBQSR_SPARK"
    script "modules/nf-core/gatk4/applybqsrspark/main.nf"
    process "GATK4_APPLYBQSR_SPARK"
    config "tests/modules/nf-core/gatk4_applybqsr_spark/nextflow.config"
    tag "gatk4_applybqsr_spark"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
