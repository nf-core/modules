nextflow_process {

    name "Test Process GATK4_INTERVALLISTTOBED"
    script "modules/nf-core/gatk4/intervallisttobed/main.nf"
    process "GATK4_INTERVALLISTTOBED"
    config "tests/modules/nf-core/gatk4_intervallisttobed/nextflow.config"
    tag "gatk4_intervallisttobed"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
