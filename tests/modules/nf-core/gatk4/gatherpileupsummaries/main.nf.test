nextflow_process {

    name "Test Process GATK4_GATHERPILEUPSUMMARIES"
    script "modules/nf-core/gatk4/gatherpileupsummaries/main.nf"
    process "GATK4_GATHERPILEUPSUMMARIES"
    config "tests/modules/nf-core/gatk4_gatherpileupsummaries/nextflow.config"
    tag "gatk4_gatherpileupsummaries"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
