nextflow_process {

    name "Test Process GATK4_PRINTREADS"
    script "modules/nf-core/gatk4/printreads/main.nf"
    process "GATK4_PRINTREADS"
    config "tests/modules/nf-core/gatk4_printreads/nextflow.config"
    tag "gatk4_printreads"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
