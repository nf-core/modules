nextflow_process {

    name "Test Process GATK4_FILTERMUTECTCALLS"
    script "modules/nf-core/gatk4/filtermutectcalls/main.nf"
    process "GATK4_FILTERMUTECTCALLS"
    config "tests/modules/nf-core/gatk4_filtermutectcalls/nextflow.config"
    tag "gatk4_filtermutectcalls"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
