nextflow_process {

    name "Test Process GATK4_SPLITCRAM"
    script "modules/nf-core/gatk4/splitcram/main.nf"
    process "GATK4_SPLITCRAM"
    config "tests/modules/nf-core/gatk4_splitcram/nextflow.config"
    tag "gatk4_splitcram"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
