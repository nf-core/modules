nextflow_process {

    name "Test Process GATK4_LEARNREADORIENTATIONMODEL"
    script "modules/nf-core/gatk4/learnreadorientationmodel/main.nf"
    process "GATK4_LEARNREADORIENTATIONMODEL"
    config "tests/modules/nf-core/gatk4_learnreadorientationmodel/nextflow.config"
    tag "gatk4_learnreadorientationmodel"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
