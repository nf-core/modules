nextflow_process {

    name "Test Process GATK4_SPLITNCIGARREADS"
    script "modules/nf-core/gatk4/splitncigarreads/main.nf"
    process "GATK4_SPLITNCIGARREADS"
    config "tests/modules/nf-core/gatk4_splitncigarreads/nextflow.config"
    tag "gatk4_splitncigarreads"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
