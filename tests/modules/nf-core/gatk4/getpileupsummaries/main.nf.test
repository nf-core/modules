nextflow_process {

    name "Test Process GATK4_GETPILEUPSUMMARIES"
    script "modules/nf-core/gatk4/getpileupsummaries/main.nf"
    process "GATK4_GETPILEUPSUMMARIES"
    config "tests/modules/nf-core/gatk4_getpileupsummaries/nextflow.config"
    tag "gatk4_getpileupsummaries"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
