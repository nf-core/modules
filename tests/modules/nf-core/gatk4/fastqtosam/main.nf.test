nextflow_process {

    name "Test Process GATK4_FASTQTOSAM"
    script "modules/nf-core/gatk4/fastqtosam/main.nf"
    process "GATK4_FASTQTOSAM"
    config "tests/modules/nf-core/gatk4_fastqtosam/nextflow.config"
    tag "gatk4_fastqtosam"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
