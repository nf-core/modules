nextflow_process {

    name "Test Process GATK4_ANNOTATEINTERVALS"
    script "modules/nf-core/gatk4/annotateintervals/main.nf"
    process "GATK4_ANNOTATEINTERVALS"
    config "tests/modules/nf-core/gatk4_annotateintervals/nextflow.config"
    tag "gatk4_annotateintervals"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
