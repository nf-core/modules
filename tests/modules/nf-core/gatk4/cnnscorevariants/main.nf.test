nextflow_process {

    name "Test Process GATK4_CNNSCOREVARIANTS"
    script "modules/nf-core/gatk4/cnnscorevariants/main.nf"
    process "GATK4_CNNSCOREVARIANTS"
    config "tests/modules/nf-core/gatk4_cnnscorevariants/nextflow.config"
    tag "gatk4_cnnscorevariants"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
