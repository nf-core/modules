nextflow_process {

    name "Test Process GATK4_COLLECTREADCOUNTS"
    script "modules/nf-core/gatk4/collectreadcounts/main.nf"
    process "GATK4_COLLECTREADCOUNTS"
    config "tests/modules/nf-core/gatk4_collectreadcounts/nextflow.config"
    tag "gatk4_collectreadcounts"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
