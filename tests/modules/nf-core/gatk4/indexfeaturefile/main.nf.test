nextflow_process {

    name "Test Process GATK4_INDEXFEATUREFILE"
    script "modules/nf-core/gatk4/indexfeaturefile/main.nf"
    process "GATK4_INDEXFEATUREFILE"
    config "tests/modules/nf-core/gatk4_indexfeaturefile/nextflow.config"
    tag "gatk4_indexfeaturefile"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
