nextflow_process {

    name "Test Process GATK4_LEFTALIGNANDTRIMVARIANTS"
    script "modules/nf-core/gatk4/leftalignandtrimvariants/main.nf"
    process "GATK4_LEFTALIGNANDTRIMVARIANTS"
    config "tests/modules/nf-core/gatk4_leftalignandtrimvariants/nextflow.config"
    tag "gatk4_leftalignandtrimvariants"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
