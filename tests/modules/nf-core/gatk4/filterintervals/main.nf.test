nextflow_process {

    name "Test Process GATK4_FILTERINTERVALS"
    script "modules/nf-core/gatk4/filterintervals/main.nf"
    process "GATK4_FILTERINTERVALS"
    config "tests/modules/nf-core/gatk4_filterintervals/nextflow.config"
    tag "gatk4_filterintervals"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
