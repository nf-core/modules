nextflow_process {

    name "Test Process GATK4_MERGEBAMALIGNMENT"
    script "modules/nf-core/gatk4/mergebamalignment/main.nf"
    process "GATK4_MERGEBAMALIGNMENT"
    config "tests/modules/nf-core/gatk4_mergebamalignment/nextflow.config"
    tag "gatk4_mergebamalignment"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
