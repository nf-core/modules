nextflow_process {

    name "Test Process GATK4_BASERECALIBRATOR_SPARK"
    script "modules/nf-core/gatk4/baserecalibratorspark/main.nf"
    process "GATK4_BASERECALIBRATOR_SPARK"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
