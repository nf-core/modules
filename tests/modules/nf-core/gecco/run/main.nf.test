nextflow_process {

    name "Test Process GECCO_RUN"
    script "modules/nf-core/gecco/run/main.nf"
    process "GECCO_RUN"
    config "tests/modules/nf-core/gecco_run/nextflow.config"
    tag "gecco_run"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
