nextflow_process {

    name "Test Process DSHBIO_FILTERGFF3"
    script "modules/nf-core/dshbio/filtergff3/main.nf"
    process "DSHBIO_FILTERGFF3"
    config "tests/modules/nf-core/dshbio_filtergff3/nextflow.config"
    tag "dshbio_filtergff3"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
