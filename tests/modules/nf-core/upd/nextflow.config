process {
    ext.args = { "--proband earlycasualcaiman --father hugelymodelbat --mother slowlycivilbuck" --af-tag AF }
    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }
    
}