nextflow_process {

    name "Test Process FASTK_FASTK"
    script "modules/nf-core/fastk/fastk/main.nf"
    process "FASTK_FASTK"
    config "tests/modules/nf-core/fastk_fastk/nextflow.config"
    tag "fastk_fastk"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
