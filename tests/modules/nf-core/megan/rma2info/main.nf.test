nextflow_process {

    name "Test Process MEGAN_RMA2INFO"
    script "modules/nf-core/megan/rma2info/main.nf"
    process "MEGAN_RMA2INFO"
    config "tests/modules/nf-core/megan_rma2info/nextflow.config"
    tag "megan_rma2info"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
