nextflow_process {

    name "Test Process LIMA"
    script "modules/nf-core/lima/main.nf"
    process "LIMA"
    config "tests/modules/nf-core/lima/nextflow.config"
    tag "lima"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
