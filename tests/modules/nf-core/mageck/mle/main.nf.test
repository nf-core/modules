nextflow_process {

    name "Test Process MAGECK_MLE"
    script "modules/nf-core/mageck/mle/main.nf"
    process "MAGECK_MLE"
    config "tests/modules/nf-core/mageck_mle/nextflow.config"
    tag "mageck_mle"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
