nextflow_process {

    name "Test Process MAGECK_COUNT"
    script "modules/nf-core/mageck/count/main.nf"
    process "MAGECK_COUNT"
    config "tests/modules/nf-core/mageck_count/nextflow.config"
    tag "mageck_count"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
