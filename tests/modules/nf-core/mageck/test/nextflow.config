process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: MAGECK_TEST {
        ext.args = "-t 'HL60.initial' "
    }

    withName: MAGECK_TEST_DAY0_LABEL {
        ext.args = "--day0-label 'HL60.initial' "
    }
}
