nextflow_process {

    name "Test Process HMMCOPY_GENERATEMAP"
    script "modules/nf-core/hmmcopy/generatemap/main.nf"
    process "HMMCOPY_GENERATEMAP"
    config "tests/modules/nf-core/hmmcopy_generatemap/nextflow.config"
    tag "hmmcopy_generatemap"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
