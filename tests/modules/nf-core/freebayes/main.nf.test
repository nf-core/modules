nextflow_process {

    name "Test Process FREEBAYES"
    script "modules/nf-core/freebayes/main.nf"
    process "FREEBAYES"
    config "tests/modules/nf-core/freebayes/nextflow.config"
    tag "freebayes"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
