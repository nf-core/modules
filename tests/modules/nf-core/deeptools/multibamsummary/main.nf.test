nextflow_process {

    name "Test Process DEEPTOOLS_MULTIBAMSUMMARY"
    script "modules/nf-core/deeptools/multibamsummary/main.nf"
    process "DEEPTOOLS_MULTIBAMSUMMARY"
    config "tests/modules/nf-core/deeptools_multibamsummary/nextflow.config"
    tag "deeptools_multibamsummary"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
