nextflow_process {

    name "Test Process DEEPTOOLS_PLOTPROFILE"
    script "modules/nf-core/deeptools/plotprofile/main.nf"
    process "DEEPTOOLS_PLOTPROFILE"
    config "tests/modules/nf-core/deeptools_plotprofile/nextflow.config"
    tag "deeptools_plotprofile"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
