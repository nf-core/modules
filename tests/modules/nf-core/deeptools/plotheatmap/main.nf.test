nextflow_process {

    name "Test Process DEEPTOOLS_PLOTHEATMAP"
    script "modules/nf-core/deeptools/plotheatmap/main.nf"
    process "DEEPTOOLS_PLOTHEATMAP"
    config "tests/modules/nf-core/deeptools_plotheatmap/nextflow.config"
    tag "deeptools_plotheatmap"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
