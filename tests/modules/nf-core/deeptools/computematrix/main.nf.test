nextflow_process {

    name "Test Process DEEPTOOLS_COMPUTEMATRIX"
    script "modules/nf-core/deeptools/computematrix/main.nf"
    process "DEEPTOOLS_COMPUTEMATRIX"
    config "tests/modules/nf-core/deeptools_computematrix/nextflow.config"
    tag "deeptools_computematrix"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
