nextflow_process {

    name "Test Process CHECKV_DOWNLOADDATABASE"
    script "modules/nf-core/checkv/downloaddatabase/main.nf"
    process "CHECKV_DOWNLOADDATABASE"
    config "tests/modules/nf-core/checkv_downloaddatabase/nextflow.config"
    tag "checkv_downloaddatabase"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
