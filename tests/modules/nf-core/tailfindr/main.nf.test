nextflow_process {

    name "Test Process TAILFINDR"
    script "modules/nf-core/tailfindr/main.nf"
    process "TAILFINDR"
    config "tests/modules/nf-core/tailfindr/nextflow.config"
    tag "tailfindr"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
