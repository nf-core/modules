nextflow_process {

    name "Test Process TRIMGALORE"
    script "modules/nf-core/trimgalore/main.nf"
    process "TRIMGALORE"
    config "tests/modules/nf-core/trimgalore/nextflow.config"
    tag "trimgalore"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
