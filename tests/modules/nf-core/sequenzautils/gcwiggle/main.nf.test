nextflow_process {

    name "Test Process SEQUENZAUTILS_GCWIGGLE"
    script "modules/nf-core/sequenzautils/gcwiggle/main.nf"
    process "SEQUENZAUTILS_GCWIGGLE"
    config "tests/modules/nf-core/sequenzautils_gcwiggle/nextflow.config"
    tag "sequenzautils_gcwiggle"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
