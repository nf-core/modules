nextflow_process {

    name "Test Process SEQUENZAUTILS_BAM2SEQZ"
    script "modules/nf-core/sequenzautils/bam2seqz/main.nf"
    process "SEQUENZAUTILS_BAM2SEQZ"
    config "tests/modules/nf-core/sequenzautils_bam2seqz/nextflow.config"
    tag "sequenzautils_bam2seqz"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
