nextflow_process {

    name "Test Process EXPANSIONHUNTERDENOVO_PROFILE"
    script "modules/nf-core/expansionhunterdenovo/profile/main.nf"
    process "EXPANSIONHUNTERDENOVO_PROFILE"
    config "tests/modules/nf-core/expansionhunterdenovo_profile/nextflow.config"
    tag "expansionhunterdenovo_profile"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
