nextflow_process {

    name "Test Process SNPSIFT_SPLIT"
    script "modules/nf-core/snpsift/split/main.nf"
    process "SNPSIFT_SPLIT"
    config "tests/modules/nf-core/snpsift_split/nextflow.config"
    tag "snpsift_split"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
