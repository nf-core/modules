nextflow_process {

    name "Test Process SEACR_CALLPEAK"
    script "modules/nf-core/seacr/callpeak/main.nf"
    process "SEACR_CALLPEAK"
    config "tests/modules/nf-core/seacr_callpeak/nextflow.config"
    tag "seacr_callpeak"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
