nextflow_process {

    name "Test Process PBCCS"
    script "modules/nf-core/pbccs/main.nf"
    process "PBCCS"
    config "tests/modules/nf-core/pbccs/nextflow.config"
    tag "pbccs"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
