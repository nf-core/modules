nextflow_process {

    name "Test Process MASHTREE"
    script "modules/nf-core/mashtree/main.nf"
    process "MASHTREE"
    config "tests/modules/nf-core/mashtree/nextflow.config"
    tag "mashtree"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
