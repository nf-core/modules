nextflow_process {

    name "Test Process YAHS"
    script "modules/nf-core/yahs/main.nf"
    process "YAHS"
    config "tests/modules/nf-core/yahs/nextflow.config"
    tag "yahs"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
