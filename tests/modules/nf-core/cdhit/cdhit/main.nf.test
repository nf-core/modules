nextflow_process {

    name "Test Process CDHIT_CDHIT"
    script "modules/nf-core/cdhit/cdhit/main.nf"
    process "CDHIT_CDHIT"
    config "tests/modules/nf-core/cdhit_cdhit/nextflow.config"
    tag "cdhit_cdhit"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
