nextflow_process {

    name "Test Process ATLAS_CALL"
    script "modules/nf-core/atlas/call/main.nf"
    process "ATLAS_CALL"
    config "tests/modules/nf-core/atlas_call/nextflow.config"
    tag "atlas_call"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
