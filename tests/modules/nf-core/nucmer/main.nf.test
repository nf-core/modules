nextflow_process {

    name "Test Process NUCMER"
    script "modules/nf-core/nucmer/main.nf"
    process "NUCMER"
    config "tests/modules/nf-core/nucmer/nextflow.config"
    tag "nucmer"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
