nextflow_process {

    name "Test Process BISMARK_DEDUPLICATE"
    script "modules/nf-core/bismark/deduplicate/main.nf"
    process "BISMARK_DEDUPLICATE"
    config "tests/modules/nf-core/bismark_deduplicate/nextflow.config"
    tag "bismark_deduplicate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
