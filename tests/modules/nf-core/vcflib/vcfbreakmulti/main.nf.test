nextflow_process {

    name "Test Process VCFLIB_VCFBREAKMULTI"
    script "modules/nf-core/vcflib/vcfbreakmulti/main.nf"
    process "VCFLIB_VCFBREAKMULTI"
    config "tests/modules/nf-core/vcflib_vcfbreakmulti/nextflow.config"
    tag "vcflib_vcfbreakmulti"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
