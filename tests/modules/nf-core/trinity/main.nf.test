nextflow_process {

    name "Test Process TRINITY"
    script "modules/nf-core/trinity/main.nf"
    process "TRINITY"
    config "tests/modules/nf-core/trinity/nextflow.config"
    tag "trinity"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
