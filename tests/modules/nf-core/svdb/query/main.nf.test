nextflow_process {

    name "Test Process SVDB_QUERY"
    script "modules/nf-core/svdb/query/main.nf"
    process "SVDB_QUERY"
    config "tests/modules/nf-core/svdb_query/nextflow.config"
    tag "svdb_query"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
