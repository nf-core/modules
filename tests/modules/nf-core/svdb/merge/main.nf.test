nextflow_process {

    name "Test Process SVDB_MERGE"
    script "modules/nf-core/svdb/merge/main.nf"
    process "SVDB_MERGE"
    config "tests/modules/nf-core/svdb_merge/nextflow.config"
    tag "svdb_merge"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
