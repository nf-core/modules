nextflow_process {

    name "Test Process METHYLDACKEL_EXTRACT"
    script "modules/nf-core/methyldackel/extract/main.nf"
    process "METHYLDACKEL_EXTRACT"
    config "tests/modules/nf-core/methyldackel_extract/nextflow.config"
    tag "methyldackel_extract"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
