nextflow_process {

    name "Test Process RSEQC_JUNCTIONSATURATION"
    script "modules/nf-core/rseqc/junctionsaturation/main.nf"
    process "RSEQC_JUNCTIONSATURATION"
    config "tests/modules/nf-core/rseqc_junctionsaturation/nextflow.config"
    tag "rseqc_junctionsaturation"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
