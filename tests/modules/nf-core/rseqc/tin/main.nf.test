nextflow_process {

    name "Test Process RSEQC_TIN"
    script "modules/nf-core/rseqc/tin/main.nf"
    process "RSEQC_TIN"
    config "tests/modules/nf-core/rseqc_tin/nextflow.config"
    tag "rseqc_tin"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
