nextflow_process {

    name "Test Process RSEQC_INFEREXPERIMENT"
    script "modules/nf-core/rseqc/inferexperiment/main.nf"
    process "RSEQC_INFEREXPERIMENT"
    config "tests/modules/nf-core/rseqc_inferexperiment/nextflow.config"
    tag "rseqc_inferexperiment"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
