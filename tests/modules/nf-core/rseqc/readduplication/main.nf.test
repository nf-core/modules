nextflow_process {

    name "Test Process RSEQC_READDUPLICATION"
    script "modules/nf-core/rseqc/readduplication/main.nf"
    process "RSEQC_READDUPLICATION"
    config "tests/modules/nf-core/rseqc_readduplication/nextflow.config"
    tag "rseqc_readduplication"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
