nextflow_process {

    name "Test Process RSEQC_READDISTRIBUTION"
    script "modules/nf-core/rseqc/readdistribution/main.nf"
    process "RSEQC_READDISTRIBUTION"
    config "tests/modules/nf-core/rseqc_readdistribution/nextflow.config"
    tag "rseqc_readdistribution"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
