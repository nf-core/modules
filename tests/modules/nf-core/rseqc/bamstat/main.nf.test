nextflow_process {

    name "Test Process RSEQC_BAMSTAT"
    script "modules/nf-core/rseqc/bamstat/main.nf"
    process "RSEQC_BAMSTAT"
    config "tests/modules/nf-core/rseqc_bamstat/nextflow.config"
    tag "rseqc_bamstat"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
