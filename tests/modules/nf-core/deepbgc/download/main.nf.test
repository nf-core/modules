nextflow_process {

    name "Test Process DEEPBGC_DOWNLOAD"
    script "modules/nf-core/deepbgc/download/main.nf"
    process "DEEPBGC_DOWNLOAD"
    config "tests/modules/nf-core/deepbgc_download/nextflow.config"
    tag "deepbgc_download"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
