nextflow_process {

    name "Test Process VERIFYBAMID_VERIFYBAMID"
    script "modules/nf-core/verifybamid/verifybamid/main.nf"
    process "VERIFYBAMID_VERIFYBAMID"
    config "tests/modules/nf-core/verifybamid_verifybamid/nextflow.config"
    tag "verifybamid_verifybamid"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
