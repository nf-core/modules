nextflow_process {

    name "Test Process BUSCO"
    script "modules/nf-core/busco/main.nf"
    process "BUSCO"
    config "tests/modules/nf-core/busco/nextflow.config"
    tag "busco"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
