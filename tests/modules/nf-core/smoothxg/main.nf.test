nextflow_process {

    name "Test Process SMOOTHXG"
    script "modules/nf-core/smoothxg/main.nf"
    process "SMOOTHXG"
    config "tests/modules/nf-core/smoothxg/nextflow.config"
    tag "smoothxg"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
