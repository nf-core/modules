nextflow_process {

    name "Test Process KRONA_KRONADB"
    script "modules/nf-core/krona/kronadb/main.nf"
    process "KRONA_KRONADB"
    config "tests/modules/nf-core/krona_kronadb/nextflow.config"
    tag "krona_kronadb"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
