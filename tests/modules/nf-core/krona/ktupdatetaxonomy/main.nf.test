nextflow_process {

    name "Test Process KRONA_KTUPDATETAXONOMY"
    script "modules/nf-core/krona/ktupdatetaxonomy/main.nf"
    process "KRONA_KTUPDATETAXONOMY"
    config "tests/modules/nf-core/krona_ktupdatetaxonomy/nextflow.config"
    tag "krona_ktupdatetaxonomy"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
