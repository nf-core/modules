nextflow_process {

    name "Test Process KRONA_KTIMPORTTEXT"
    script "modules/nf-core/krona/ktimporttext/main.nf"
    process "KRONA_KTIMPORTTEXT"
    config "tests/modules/nf-core/krona_ktimporttext/nextflow.config"
    tag "krona_ktimporttext"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
