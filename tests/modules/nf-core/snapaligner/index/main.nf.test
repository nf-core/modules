nextflow_process {

    name "Test Process SNAPALIGNER_INDEX"
    script "modules/nf-core/snapaligner/index/main.nf"
    process "SNAPALIGNER_INDEX"
    config "tests/modules/nf-core/snapaligner_index/nextflow.config"
    tag "snapaligner_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
