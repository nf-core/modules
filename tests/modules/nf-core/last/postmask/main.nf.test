nextflow_process {

    name "Test Process LAST_POSTMASK"
    script "modules/nf-core/last/postmask/main.nf"
    process "LAST_POSTMASK"
    config "tests/modules/nf-core/last_postmask/nextflow.config"
    tag "last_postmask"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
