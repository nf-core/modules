nextflow_process {

    name "Test Process LAST_SPLIT"
    script "modules/nf-core/last/split/main.nf"
    process "LAST_SPLIT"
    config "tests/modules/nf-core/last_split/nextflow.config"
    tag "last_split"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
