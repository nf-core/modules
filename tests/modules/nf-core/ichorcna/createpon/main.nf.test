nextflow_process {

    name "Test Process ICHORCNA_CREATEPON"
    script "modules/nf-core/ichorcna/createpon/main.nf"
    process "ICHORCNA_CREATEPON"
    config "tests/modules/nf-core/ichorcna_createpon/nextflow.config"
    tag "ichorcna_createpon"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
