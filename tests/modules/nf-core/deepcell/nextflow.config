process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: "DEEPCELL" {
        ext.args = '--image-mpp=0.1156 --compartment=whole-cell --nuclear-channel 1 --membrane-channel 0'
    }

}

docker.runOptions = '--entrypoint ""'
