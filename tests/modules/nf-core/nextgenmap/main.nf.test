nextflow_process {

    name "Test Process NEXTGENMAP"
    script "modules/nf-core/nextgenmap/main.nf"
    process "NEXTGENMAP"
    config "tests/modules/nf-core/nextgenmap/nextflow.config"
    tag "nextgenmap"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
