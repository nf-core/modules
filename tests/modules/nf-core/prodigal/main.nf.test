nextflow_process {

    name "Test Process PRODIGAL"
    script "modules/nf-core/prodigal/main.nf"
    process "PRODIGAL"
    config "tests/modules/nf-core/prodigal/nextflow.config"
    tag "prodigal"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
