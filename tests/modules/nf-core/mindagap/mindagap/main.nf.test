nextflow_process {

    name "Test Process MINDAGAP_MINDAGAP"
    script "modules/nf-core/mindagap/mindagap/main.nf"
    process "MINDAGAP_MINDAGAP"
    config "tests/modules/nf-core/mindagap_mindagap/nextflow.config"
    tag "mindagap_mindagap"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
