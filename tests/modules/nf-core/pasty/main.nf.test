nextflow_process {

    name "Test Process PASTY"
    script "modules/nf-core/pasty/main.nf"
    process "PASTY"
    config "tests/modules/nf-core/pasty/nextflow.config"
    tag "pasty"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
