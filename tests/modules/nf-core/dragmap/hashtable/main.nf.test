nextflow_process {

    name "Test Process DRAGMAP_HASHTABLE"
    script "modules/nf-core/dragmap/hashtable/main.nf"
    process "DRAGMAP_HASHTABLE"
    config "tests/modules/nf-core/dragmap_hashtable/nextflow.config"
    tag "dragmap_hashtable"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
