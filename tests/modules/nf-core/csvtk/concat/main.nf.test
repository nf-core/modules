nextflow_process {

    name "Test Process CSVTK_CONCAT"
    script "modules/nf-core/csvtk/concat/main.nf"
    process "CSVTK_CONCAT"
    config "tests/modules/nf-core/csvtk_concat/nextflow.config"
    tag "csvtk_concat"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
