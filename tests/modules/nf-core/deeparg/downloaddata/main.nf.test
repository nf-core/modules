nextflow_process {

    name "Test Process DEEPARG_DOWNLOADDATA"
    script "modules/nf-core/deeparg/downloaddata/main.nf"
    process "DEEPARG_DOWNLOADDATA"
    config "tests/modules/nf-core/deeparg_downloaddata/nextflow.config"
    tag "deeparg_downloaddata"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
