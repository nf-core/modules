nextflow_process {

    name "Test Process BAKTA_BAKTA"
    script "modules/nf-core/bakta/bakta/main.nf"
    process "BAKTA_BAKTA"
    config "tests/modules/nf-core/bakta_bakta/nextflow.config"
    tag "bakta_bakta"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
