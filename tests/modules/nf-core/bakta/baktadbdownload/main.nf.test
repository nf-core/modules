nextflow_process {

    name "Test Process BAKTA_BAKTADBDOWNLOAD"
    script "modules/nf-core/bakta/baktadbdownload/main.nf"
    process "BAKTA_BAKTADBDOWNLOAD"
    config "tests/modules/nf-core/bakta_baktadbdownload/nextflow.config"
    tag "bakta_baktadbdownload"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
