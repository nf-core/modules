nextflow_process {

    name "Test Process UCSC_BEDGRAPHTOBIGWIG"
    script "modules/nf-core/ucsc/bedgraphtobigwig/main.nf"
    process "UCSC_BEDGRAPHTOBIGWIG"
    config "tests/modules/nf-core/ucsc_bedgraphtobigwig/nextflow.config"
    tag "ucsc_bedgraphtobigwig"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
