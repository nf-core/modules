nextflow_process {

    name "Test Process UCSC_GTFTOGENEPRED"
    script "modules/nf-core/ucsc/gtftogenepred/main.nf"
    process "UCSC_GTFTOGENEPRED"
    config "tests/modules/nf-core/ucsc_gtftogenepred/nextflow.config"
    tag "ucsc_gtftogenepred"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
