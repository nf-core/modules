nextflow_process {

    name "Test Process PBPTYPER"
    script "modules/nf-core/pbptyper/main.nf"
    process "PBPTYPER"
    config "tests/modules/nf-core/pbptyper/nextflow.config"
    tag "pbptyper"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
