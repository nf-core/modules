nextflow_process {

    name "Test Process CHROMAP_INDEX"
    script "modules/nf-core/chromap/index/main.nf"
    process "CHROMAP_INDEX"
    config "tests/modules/nf-core/chromap_index/nextflow.config"
    tag "chromap_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
