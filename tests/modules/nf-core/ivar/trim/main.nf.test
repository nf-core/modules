nextflow_process {

    name "Test Process IVAR_TRIM"
    script "modules/nf-core/ivar/trim/main.nf"
    process "IVAR_TRIM"
    config "tests/modules/nf-core/ivar_trim/nextflow.config"
    tag "ivar_trim"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
