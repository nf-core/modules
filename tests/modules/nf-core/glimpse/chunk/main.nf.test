nextflow_process {

    name "Test Process GLIMPSE_CHUNK"
    script "modules/nf-core/glimpse/chunk/main.nf"
    process "GLIMPSE_CHUNK"
    config "tests/modules/nf-core/glimpse_chunk/nextflow.config"
    tag "glimpse_chunk"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
