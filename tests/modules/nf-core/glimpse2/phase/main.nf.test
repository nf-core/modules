nextflow_process {

    name "Test Process GLIMPSE2_PHASE"
    script "modules/nf-core/glimpse2/phase/main.nf"
    process "GLIMPSE2_PHASE"
    config "tests/modules/nf-core/glimpse2_phase/nextflow.config"
    tag "glimpse2_phase"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
