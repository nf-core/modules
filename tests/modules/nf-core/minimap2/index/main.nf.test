nextflow_process {

    name "Test Process MINIMAP2_INDEX"
    script "modules/nf-core/minimap2/index/main.nf"
    process "MINIMAP2_INDEX"
    config "tests/modules/nf-core/minimap2_index/nextflow.config"
    tag "minimap2_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
