nextflow_process {

    name "Test Process MINIMAP2_ALIGN"
    script "modules/nf-core/minimap2/align/main.nf"
    process "MINIMAP2_ALIGN"
    config "tests/modules/nf-core/minimap2_align/nextflow.config"
    tag "minimap2_align"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
