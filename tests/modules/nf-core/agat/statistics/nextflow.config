process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: 'test_basic:AGAT_STATISTICS' {
        ext.args = "--basic"
    }

    withName: 'test_functional:AGAT_STATISTICS' {
        ext.args = "--functional"
    }

}
