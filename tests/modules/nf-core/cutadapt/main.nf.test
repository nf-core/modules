nextflow_process {

    name "Test Process CUTADAPT"
    script "modules/nf-core/cutadapt/main.nf"
    process "CUTADAPT"
    config "tests/modules/nf-core/cutadapt/nextflow.config"
    tag "cutadapt"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
