nextflow_process {

    name "Test Process CELLRANGER_MKGTF"
    script "modules/nf-core/cellranger/mkgtf/main.nf"
    process "CELLRANGER_MKGTF"
    config "tests/modules/nf-core/cellranger_mkgtf/nextflow.config"
    tag "cellranger_mkgtf"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
