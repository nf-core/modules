nextflow_process {

    name "Test Process QUALIMAP_BAMQCCRAM"
    script "modules/nf-core/qualimap/bamqccram/main.nf"
    process "QUALIMAP_BAMQCCRAM"
    config "tests/modules/nf-core/qualimap_bamqccram/nextflow.config"
    tag "qualimap_bamqccram"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
