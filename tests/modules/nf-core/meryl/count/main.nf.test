nextflow_process {

    name "Test Process MERYL_COUNT"
    script "modules/nf-core/meryl/count/main.nf"
    process "MERYL_COUNT"
    config "tests/modules/nf-core/meryl_count/nextflow.config"
    tag "meryl_count"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
