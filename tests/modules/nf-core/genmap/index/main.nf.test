nextflow_process {

    name "Test Process GENMAP_INDEX"
    script "modules/nf-core/genmap/index/main.nf"
    process "GENMAP_INDEX"
    config "tests/modules/nf-core/genmap_index/nextflow.config"
    tag "genmap_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
