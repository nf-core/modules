nextflow_process {

    name "Test Process HOMER_ANNOTATEPEAKS"
    script "modules/nf-core/homer/annotatepeaks/main.nf"
    process "HOMER_ANNOTATEPEAKS"
    config "tests/modules/nf-core/homer_annotatepeaks/nextflow.config"
    tag "homer_annotatepeaks"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
