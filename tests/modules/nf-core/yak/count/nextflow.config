process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: 'test_yak_count_se:YAK_COUNT' {
        ext.prefix = { "${meta.id}" }
        ext.args = ''
    }

    withName: 'test_yak_count_pe:YAK_COUNT' {
        ext.prefix = { "${meta.id}" }
        ext.args = ''
    }   
}
