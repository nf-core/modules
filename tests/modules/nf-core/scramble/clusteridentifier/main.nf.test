nextflow_process {

    name "Test Process SCRAMBLE_CLUSTERIDENTIFIER"
    script "modules/nf-core/scramble/clusteridentifier/main.nf"
    process "SCRAMBLE_CLUSTERIDENTIFIER"
    config "tests/modules/nf-core/scramble_clusteridentifier/nextflow.config"
    tag "scramble_clusteridentifier"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
