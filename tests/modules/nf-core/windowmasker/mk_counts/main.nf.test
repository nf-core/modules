nextflow_process {

    name "Test Process WINDOWMASKER_MKCOUNTS"
    script "modules/nf-core/windowmasker/mk_counts/main.nf"
    process "WINDOWMASKER_MKCOUNTS"
    config "tests/modules/nf-core/windowmasker_mkcounts/nextflow.config"
    tag "windowmasker_mkcounts"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
