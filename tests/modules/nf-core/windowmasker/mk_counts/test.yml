- name: windowmasker test_windowmasker_mkcounts
  command: nextflow run main.nf -entry test_windowmasker_mkcounts -c ../../../../config/nextflow.config -c nextflow.config
  tags:
    - windowmasker
  files:
    - path: output/windowmasker/test.interval
      md5sum: 2e8a3ece921560393ec36426fbcfb6ed
    - path: output/miniprot/versions.yml

        arg_list = task.ext.args.split('-')
        for i in arg_list:
            if i.contains('outfmt'):
                outfmt = i.split(' ')[1] ?: "interval"
            else:
                pass