nextflow_process {

    name "Test Process HAMRONIZATION_ABRICATE"
    script "modules/nf-core/hamronization/abricate/main.nf"
    process "HAMRONIZATION_ABRICATE"
    config "tests/modules/nf-core/hamronization_abricate/nextflow.config"
    tag "hamronization_abricate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
