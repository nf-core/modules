nextflow_process {

    name "Test Process FASTAWINDOWS"
    script "modules/nf-core/fastawindows/main.nf"
    process "FASTAWINDOWS"
    config "tests/modules/nf-core/fastawindows/nextflow.config"
    tag "fastawindows"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
