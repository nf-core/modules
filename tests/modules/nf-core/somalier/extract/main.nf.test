nextflow_process {

    name "Test Process SOMALIER_EXTRACT"
    script "modules/nf-core/somalier/extract/main.nf"
    process "SOMALIER_EXTRACT"
    config "tests/modules/nf-core/somalier_extract/nextflow.config"
    tag "somalier_extract"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
