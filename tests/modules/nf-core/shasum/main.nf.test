nextflow_process {

    name "Test Process SHASUM"
    script "modules/nf-core/shasum/main.nf"
    process "SHASUM"
    config "tests/modules/nf-core/shasum/nextflow.config"
    tag "shasum"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
