nextflow_process {

    name "Test Process PAIRIX"
    script "modules/nf-core/pairix/main.nf"
    process "PAIRIX"
    config "tests/modules/nf-core/pairix/nextflow.config"
    tag "pairix"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
