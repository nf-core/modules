nextflow_process {

    name "Test Process STRELKA_GERMLINE"
    script "modules/nf-core/strelka/germline/main.nf"
    process "STRELKA_GERMLINE"
    config "tests/modules/nf-core/strelka_germline/nextflow.config"
    tag "strelka_germline"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
