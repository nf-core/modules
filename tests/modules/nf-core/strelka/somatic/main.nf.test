nextflow_process {

    name "Test Process STRELKA_SOMATIC"
    script "modules/nf-core/strelka/somatic/main.nf"
    process "STRELKA_SOMATIC"
    config "tests/modules/nf-core/strelka_somatic/nextflow.config"
    tag "strelka_somatic"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
