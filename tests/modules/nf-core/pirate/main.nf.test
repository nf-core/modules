nextflow_process {

    name "Test Process PIRATE"
    script "modules/nf-core/pirate/main.nf"
    process "PIRATE"
    config "tests/modules/nf-core/pirate/nextflow.config"
    tag "pirate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
