nextflow_process {

    name "Test Process BCFTOOLS_ANNOTATE"
    script "modules/nf-core/bcftools/annotate/main.nf"
    process "BCFTOOLS_ANNOTATE"
    config "tests/modules/nf-core/bcftools_annotate/nextflow.config"
    tag "bcftools_annotate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
