nextflow_process {

    name "Test Process MCRONI"
    script "modules/nf-core/mcroni/main.nf"
    process "MCRONI"
    config "tests/modules/nf-core/mcroni/nextflow.config"
    tag "mcroni"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
