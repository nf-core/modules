nextflow_process {

    name "Test Process RSEM_PREPAREREFERENCE"
    script "modules/nf-core/rsem/preparereference/main.nf"
    process "RSEM_PREPAREREFERENCE"
    config "tests/modules/nf-core/rsem_preparereference/nextflow.config"
    tag "rsem_preparereference"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
