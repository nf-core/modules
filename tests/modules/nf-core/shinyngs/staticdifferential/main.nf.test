nextflow_process {

    name "Test Process SHINYNGS_STATICDIFFERENTIAL"
    script "modules/nf-core/shinyngs/staticdifferential/main.nf"
    process "SHINYNGS_STATICDIFFERENTIAL"
    config "tests/modules/nf-core/shinyngs_staticdifferential/nextflow.config"
    tag "shinyngs_staticdifferential"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
