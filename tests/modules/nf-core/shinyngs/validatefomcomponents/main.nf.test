nextflow_process {

    name "Test Process SHINYNGS_VALIDATEFOMCOMPONENTS"
    script "modules/nf-core/shinyngs/validatefomcomponents/main.nf"
    process "SHINYNGS_VALIDATEFOMCOMPONENTS"
    config "tests/modules/nf-core/shinyngs_validatefomcomponents/nextflow.config"
    tag "shinyngs_validatefomcomponents"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
