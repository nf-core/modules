nextflow_process {

    name "Test Process SHINYNGS_APP"
    script "modules/nf-core/shinyngs/app/main.nf"
    process "SHINYNGS_APP"
    config "tests/modules/nf-core/shinyngs_app/nextflow.config"
    tag "shinyngs_app"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
