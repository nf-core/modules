nextflow_process {

    name "Test Process KAT_HIST"
    script "modules/nf-core/kat/hist/main.nf"
    process "KAT_HIST"
    config "tests/modules/nf-core/kat_hist/nextflow.config"
    tag "kat_hist"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
