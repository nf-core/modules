nextflow_process {

    name "Test Process HAPLOGREP2_CLASSIFY"
    script "modules/nf-core/haplogrep2/classify/main.nf"
    process "HAPLOGREP2_CLASSIFY"
    config "tests/modules/nf-core/haplogrep2_classify/nextflow.config"
    tag "haplogrep2_classify"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
