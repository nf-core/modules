nextflow_process {

    name "Test Process MYKROBE_PREDICT"
    script "modules/nf-core/mykrobe/predict/main.nf"
    process "MYKROBE_PREDICT"
    config "tests/modules/nf-core/mykrobe_predict/nextflow.config"
    tag "mykrobe_predict"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
