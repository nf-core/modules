nextflow_process {

    name "Test Process GSTAMA_MERGE"
    script "modules/nf-core/gstama/merge/main.nf"
    process "GSTAMA_MERGE"
    config "tests/modules/nf-core/gstama_merge/nextflow.config"
    tag "gstama_merge"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
