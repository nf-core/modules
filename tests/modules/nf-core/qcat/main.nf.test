nextflow_process {

    name "Test Process QCAT"
    script "modules/nf-core/qcat/main.nf"
    process "QCAT"
    config "tests/modules/nf-core/qcat/nextflow.config"
    tag "qcat"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
