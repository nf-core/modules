nextflow_process {

    name "Test Process FASTQSCAN"
    script "modules/nf-core/fastqscan/main.nf"
    process "FASTQSCAN"
    config "tests/modules/nf-core/fastqscan/nextflow.config"
    tag "fastqscan"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
