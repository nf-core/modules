nextflow_process {

    name "Test Process SRATOOLS_PREFETCH"
    script "modules/nf-core/sratools/prefetch/main.nf"
    process "SRATOOLS_PREFETCH"
    config "tests/modules/nf-core/sratools_prefetch/nextflow.config"
    tag "sratools_prefetch"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
