process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: UNIVERSC {
        ext.args = ''
    }

    withName: UNIVERSC_CELLRANGER_OS_COUNT {
        ext.args = '--chemistry="SC3Pv3"'
    }
    
}
