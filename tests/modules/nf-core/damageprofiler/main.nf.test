nextflow_process {

    name "Test Process DAMAGEPROFILER"
    script "modules/nf-core/damageprofiler/main.nf"
    process "DAMAGEPROFILER"
    config "tests/modules/nf-core/damageprofiler/nextflow.config"
    tag "damageprofiler"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
