nextflow_process {

    name "Test Process MCQUANT"
    script "modules/nf-core/mcquant/main.nf"
    process "MCQUANT"
    config "tests/modules/nf-core/mcquant/nextflow.config"
    tag "mcquant"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
