nextflow_process {

    name "Test Process GATK_REALIGNERTARGETCREATOR"
    script "modules/nf-core/gatk/realignertargetcreator/main.nf"
    process "GATK_REALIGNERTARGETCREATOR"
    config "tests/modules/nf-core/gatk_realignertargetcreator/nextflow.config"
    tag "gatk_realignertargetcreator"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
