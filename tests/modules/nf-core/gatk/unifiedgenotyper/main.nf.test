nextflow_process {

    name "Test Process GATK_UNIFIEDGENOTYPER"
    script "modules/nf-core/gatk/unifiedgenotyper/main.nf"
    process "GATK_UNIFIEDGENOTYPER"
    config "tests/modules/nf-core/gatk_unifiedgenotyper/nextflow.config"
    tag "gatk_unifiedgenotyper"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
