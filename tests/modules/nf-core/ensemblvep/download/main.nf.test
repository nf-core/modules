nextflow_process {

    name "Test Process ENSEMBLVEP_DOWNLOAD"
    script "modules/nf-core/ensemblvep/download/main.nf"
    process "ENSEMBLVEP_DOWNLOAD"
    config "tests/modules/nf-core/ensemblvep_download/nextflow.config"
    tag "ensemblvep_download"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
