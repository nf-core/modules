nextflow_process {

    name "Test Process MSISENSORPRO_SCAN"
    script "modules/nf-core/msisensorpro/scan/main.nf"
    process "MSISENSORPRO_SCAN"
    config "tests/modules/nf-core/msisensorpro_scan/nextflow.config"
    tag "msisensorpro_scan"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
