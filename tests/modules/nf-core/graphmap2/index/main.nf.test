nextflow_process {

    name "Test Process GRAPHMAP2_INDEX"
    script "modules/nf-core/graphmap2/index/main.nf"
    process "GRAPHMAP2_INDEX"
    config "tests/modules/nf-core/graphmap2_index/nextflow.config"
    tag "graphmap2_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
