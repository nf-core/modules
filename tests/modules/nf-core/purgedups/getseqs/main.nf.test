nextflow_process {

    name "Test Process PURGEDUPS_GETSEQS"
    script "modules/nf-core/purgedups/getseqs/main.nf"
    process "PURGEDUPS_GETSEQS"
    config "tests/modules/nf-core/purgedups_getseqs/nextflow.config"
    tag "purgedups_getseqs"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
