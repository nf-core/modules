nextflow_process {

    name "Test Process PURGEDUPS_SPLITFA"
    script "modules/nf-core/purgedups/splitfa/main.nf"
    process "PURGEDUPS_SPLITFA"
    config "tests/modules/nf-core/purgedups_splitfa/nextflow.config"
    tag "purgedups_splitfa"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
