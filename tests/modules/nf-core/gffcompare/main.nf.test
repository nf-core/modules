nextflow_process {

    name "Test Process GFFCOMPARE"
    script "modules/nf-core/gffcompare/main.nf"
    process "GFFCOMPARE"
    config "tests/modules/nf-core/gffcompare/nextflow.config"
    tag "gffcompare"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
