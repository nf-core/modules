nextflow_process {

    name "Test Process NEXTCLADE_DATASETGET"
    script "modules/nf-core/nextclade/datasetget/main.nf"
    process "NEXTCLADE_DATASETGET"
    config "tests/modules/nf-core/nextclade_datasetget/nextflow.config"
    tag "nextclade_datasetget"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
