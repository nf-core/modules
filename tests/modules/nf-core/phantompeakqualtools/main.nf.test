nextflow_process {

    name "Test Process PHANTOMPEAKQUALTOOLS"
    script "modules/nf-core/phantompeakqualtools/main.nf"
    process "PHANTOMPEAKQUALTOOLS"
    config "tests/modules/nf-core/phantompeakqualtools/nextflow.config"
    tag "phantompeakqualtools"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
