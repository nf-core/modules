nextflow_process {

    name "Test Process HAPPY_PREPY"
    script "modules/nf-core/happy/prepy/main.nf"
    process "HAPPY_PREPY"
    config "tests/modules/nf-core/happy_prepy/nextflow.config"
    tag "happy_prepy"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
