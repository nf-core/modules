nextflow_process {

    name "Test Process LOFREQ_CALLPARALLEL"
    script "modules/nf-core/lofreq/callparallel/main.nf"
    process "LOFREQ_CALLPARALLEL"
    config "tests/modules/nf-core/lofreq_callparallel/nextflow.config"
    tag "lofreq_callparallel"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
