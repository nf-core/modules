nextflow_process {

    name "Test Process LOFREQ_CALL"
    script "modules/nf-core/lofreq/call/main.nf"
    process "LOFREQ_CALL"
    config "tests/modules/nf-core/lofreq_call/nextflow.config"
    tag "lofreq_call"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
