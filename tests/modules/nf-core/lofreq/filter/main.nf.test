nextflow_process {

    name "Test Process LOFREQ_FILTER"
    script "modules/nf-core/lofreq/filter/main.nf"
    process "LOFREQ_FILTER"
    config "tests/modules/nf-core/lofreq_filter/nextflow.config"
    tag "lofreq_filter"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
