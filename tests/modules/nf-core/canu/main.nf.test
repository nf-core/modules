nextflow_process {

    name "Test Process CANU"
    script "modules/nf-core/canu/main.nf"
    process "CANU"
    config "tests/modules/nf-core/canu/nextflow.config"
    tag "canu"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
