nextflow_process {

    name "Test Process UNZIPFILES"
    script "modules/nf-core/unzipfiles/main.nf"
    process "UNZIPFILES"
    config "tests/modules/nf-core/unzipfiles/nextflow.config"
    tag "unzipfiles"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
