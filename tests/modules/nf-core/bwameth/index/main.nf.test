nextflow_process {

    name "Test Process BWAMETH_INDEX"
    script "modules/nf-core/bwameth/index/main.nf"
    process "BWAMETH_INDEX"
    config "tests/modules/nf-core/bwameth_index/nextflow.config"
    tag "bwameth_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
