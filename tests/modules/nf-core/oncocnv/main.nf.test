nextflow_process {

    name "Test Process ONCOCNV"
    script "modules/nf-core/oncocnv/main.nf"
    process "ONCOCNV"
    config "tests/modules/nf-core/oncocnv/nextflow.config"
    tag "oncocnv"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
