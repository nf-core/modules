nextflow_process {

    name "Test Process GLNEXUS"
    script "modules/nf-core/glnexus/main.nf"
    process "GLNEXUS"
    config "tests/modules/nf-core/glnexus/nextflow.config"
    tag "glnexus"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
