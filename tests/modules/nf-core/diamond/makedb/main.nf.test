nextflow_process {

    name "Test Process DIAMOND_MAKEDB"
    script "modules/nf-core/diamond/makedb/main.nf"
    process "DIAMOND_MAKEDB"
    config "tests/modules/nf-core/diamond_makedb/nextflow.config"
    tag "diamond_makedb"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
