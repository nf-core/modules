process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }
    withName: HICEXPLORER_HICPLOTMATRIX {
        ext.args = '--region chrX:3000000-3500000 --region2 chrX:3100000-3600000 --log1p --clearMaskedBins'
    }
}
