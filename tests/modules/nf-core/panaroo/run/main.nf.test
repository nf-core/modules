nextflow_process {

    name "Test Process PANAROO_RUN"
    script "modules/nf-core/panaroo/run/main.nf"
    process "PANAROO_RUN"
    config "tests/modules/nf-core/panaroo_run/nextflow.config"
    tag "panaroo_run"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
