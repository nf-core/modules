nextflow_process {

    name "Test Process SEROBA_RUN"
    script "modules/nf-core/seroba/run/main.nf"
    process "SEROBA_RUN"
    config "tests/modules/nf-core/seroba_run/nextflow.config"
    tag "seroba_run"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
