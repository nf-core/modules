nextflow_process {

    name "Test Process SCOARY"
    script "modules/nf-core/scoary/main.nf"
    process "SCOARY"
    config "tests/modules/nf-core/scoary/nextflow.config"
    tag "scoary"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
