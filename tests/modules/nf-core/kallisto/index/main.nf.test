nextflow_process {

    name "Test Process KALLISTO_INDEX"
    script "modules/nf-core/kallisto/index/main.nf"
    process "KALLISTO_INDEX"
    config "tests/modules/nf-core/kallisto_index/nextflow.config"
    tag "kallisto_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
