nextflow_process {

    name "Test Process MANTA_SOMATIC"
    script "modules/nf-core/manta/somatic/main.nf"
    process "MANTA_SOMATIC"
    config "tests/modules/nf-core/manta_somatic/nextflow.config"
    tag "manta_somatic"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
