nextflow_process {

    name "Test Process VG_CONSTRUCT"
    script "modules/nf-core/vg/construct/main.nf"
    process "VG_CONSTRUCT"
    config "tests/modules/nf-core/vg_construct/nextflow.config"
    tag "vg_construct"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
