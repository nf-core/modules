nextflow_process {

    name "Test Process COOLER_DUMP"
    script "modules/nf-core/cooler/dump/main.nf"
    process "COOLER_DUMP"
    config "tests/modules/nf-core/cooler_dump/nextflow.config"
    tag "cooler_dump"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
