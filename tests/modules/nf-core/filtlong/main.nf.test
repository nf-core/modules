nextflow_process {

    name "Test Process FILTLONG"
    script "modules/nf-core/filtlong/main.nf"
    process "FILTLONG"
    config "tests/modules/nf-core/filtlong/nextflow.config"
    tag "filtlong"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
