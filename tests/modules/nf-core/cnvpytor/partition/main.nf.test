nextflow_process {

    name "Test Process CNVPYTOR_PARTITION"
    script "modules/nf-core/cnvpytor/partition/main.nf"
    process "CNVPYTOR_PARTITION"
    config "tests/modules/nf-core/cnvpytor_partition/nextflow.config"
    tag "cnvpytor_partition"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
