nextflow_process {

    name "Test Process CNVPYTOR_IMPORTREADDEPTH"
    script "modules/nf-core/cnvpytor/importreaddepth/main.nf"
    process "CNVPYTOR_IMPORTREADDEPTH"
    config "tests/modules/nf-core/cnvpytor_importreaddepth/nextflow.config"
    tag "cnvpytor_importreaddepth"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
