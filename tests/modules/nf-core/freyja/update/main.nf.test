nextflow_process {

    name "Test Process FREYJA_UPDATE"
    script "modules/nf-core/freyja/update/main.nf"
    process "FREYJA_UPDATE"
    config "tests/modules/nf-core/freyja_update/nextflow.config"
    tag "freyja_update"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
