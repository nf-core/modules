nextflow_process {

    name "Test Process FREYJA_VARIANTS"
    script "modules/nf-core/freyja/variants/main.nf"
    process "FREYJA_VARIANTS"
    config "tests/modules/nf-core/freyja_variants/nextflow.config"
    tag "freyja_variants"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
