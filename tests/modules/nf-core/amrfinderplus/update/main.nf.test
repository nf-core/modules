nextflow_process {

    name "Test Process AMRFINDERPLUS_UPDATE"
    script "modules/nf-core/amrfinderplus/update/main.nf"
    process "AMRFINDERPLUS_UPDATE"
    config "tests/modules/nf-core/amrfinderplus_update/nextflow.config"
    tag "amrfinderplus_update"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
