nextflow_process {

    name "Test Process CUSTOM_TABULARTOGSEACLS"
    script "modules/nf-core/custom/tabulartogseacls/main.nf"
    process "CUSTOM_TABULARTOGSEACLS"
    config "tests/modules/nf-core/custom_tabulartogseacls/nextflow.config"
    tag "custom_tabulartogseacls"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
