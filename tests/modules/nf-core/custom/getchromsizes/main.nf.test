nextflow_process {

    name "Test Process CUSTOM_GETCHROMSIZES"
    script "modules/nf-core/custom/getchromsizes/main.nf"
    process "CUSTOM_GETCHROMSIZES"
    config "tests/modules/nf-core/custom_getchromsizes/nextflow.config"
    tag "custom_getchromsizes"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
