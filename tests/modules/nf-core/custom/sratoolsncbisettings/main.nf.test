nextflow_process {

    name "Test Process CUSTOM_SRATOOLSNCBISETTINGS"
    script "modules/nf-core/custom/sratoolsncbisettings/main.nf"
    process "CUSTOM_SRATOOLSNCBISETTINGS"
    config "tests/modules/nf-core/custom_sratoolsncbisettings/nextflow.config"
    tag "custom_sratoolsncbisettings"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
