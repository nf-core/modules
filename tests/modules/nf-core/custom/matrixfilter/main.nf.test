nextflow_process {

    name "Test Process CUSTOM_MATRIXFILTER"
    script "modules/nf-core/custom/matrixfilter/main.nf"
    process "CUSTOM_MATRIXFILTER"
    config "tests/modules/nf-core/custom_matrixfilter/nextflow.config"
    tag "custom_matrixfilter"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
