nextflow_process {

    name "Test Process VSEARCH_SORT"
    script "modules/nf-core/vsearch/sort/main.nf"
    process "VSEARCH_SORT"
    config "tests/modules/nf-core/vsearch_sort/nextflow.config"
    tag "vsearch_sort"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
