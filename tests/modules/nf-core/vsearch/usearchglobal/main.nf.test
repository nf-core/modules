nextflow_process {

    name "Test Process VSEARCH_USEARCHGLOBAL"
    script "modules/nf-core/vsearch/usearchglobal/main.nf"
    process "VSEARCH_USEARCHGLOBAL"
    config "tests/modules/nf-core/vsearch_usearchglobal/nextflow.config"
    tag "vsearch_usearchglobal"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
