nextflow_process {

    name "Test Process FASTTREE"
    script "modules/nf-core/fasttree/main.nf"
    process "FASTTREE"
    config "tests/modules/nf-core/fasttree/nextflow.config"
    tag "fasttree"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
