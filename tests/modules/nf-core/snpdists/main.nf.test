nextflow_process {

    name "Test Process SNPDISTS"
    script "modules/nf-core/snpdists/main.nf"
    process "SNPDISTS"
    config "tests/modules/nf-core/snpdists/nextflow.config"
    tag "snpdists"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
