nextflow_process {

    name "Test Process TAXPASTA_STANDARDISE"
    script "modules/nf-core/taxpasta/standardise/main.nf"
    process "TAXPASTA_STANDARDISE"
    config "tests/modules/nf-core/taxpasta_standardise/nextflow.config"
    tag "taxpasta_standardise"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
