nextflow_process {

    name "Test Process TAXPASTA_MERGE"
    script "modules/nf-core/taxpasta/merge/main.nf"
    process "TAXPASTA_MERGE"
    config "tests/modules/nf-core/taxpasta_merge/nextflow.config"
    tag "taxpasta_merge"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
