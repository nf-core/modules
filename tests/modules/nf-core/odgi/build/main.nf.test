nextflow_process {

    name "Test Process ODGI_BUILD"
    script "modules/nf-core/odgi/build/main.nf"
    process "ODGI_BUILD"
    config "tests/modules/nf-core/odgi_build/nextflow.config"
    tag "odgi_build"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
