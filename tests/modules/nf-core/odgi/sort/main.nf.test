nextflow_process {

    name "Test Process ODGI_SORT"
    script "modules/nf-core/odgi/sort/main.nf"
    process "ODGI_SORT"
    config "tests/modules/nf-core/odgi_sort/nextflow.config"
    tag "odgi_sort"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
