nextflow_process {

    name "Test Process ODGI_SQUEEZE"
    script "modules/nf-core/odgi/squeeze/main.nf"
    process "ODGI_SQUEEZE"
    config "tests/modules/nf-core/odgi_squeeze/nextflow.config"
    tag "odgi_squeeze"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
