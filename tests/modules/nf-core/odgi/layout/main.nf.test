nextflow_process {

    name "Test Process ODGI_LAYOUT"
    script "modules/nf-core/odgi/layout/main.nf"
    process "ODGI_LAYOUT"
    config "tests/modules/nf-core/odgi_layout/nextflow.config"
    tag "odgi_layout"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
