nextflow_process {

    name "Test Process ODGI_DRAW"
    script "modules/nf-core/odgi/draw/main.nf"
    process "ODGI_DRAW"
    config "tests/modules/nf-core/odgi_draw/nextflow.config"
    tag "odgi_draw"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
