nextflow_process {

    name "Test Process ODGI_STATS"
    script "modules/nf-core/odgi/stats/main.nf"
    process "ODGI_STATS"
    config "tests/modules/nf-core/odgi_stats/nextflow.config"
    tag "odgi_stats"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
