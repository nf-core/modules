nextflow_process {

    name "Test Process HICAP"
    script "modules/nf-core/hicap/main.nf"
    process "HICAP"
    config "tests/modules/nf-core/hicap/nextflow.config"
    tag "hicap"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
