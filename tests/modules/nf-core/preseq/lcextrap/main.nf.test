nextflow_process {

    name "Test Process PRESEQ_LCEXTRAP"
    script "modules/nf-core/preseq/lcextrap/main.nf"
    process "PRESEQ_LCEXTRAP"
    config "tests/modules/nf-core/preseq_lcextrap/nextflow.config"
    tag "preseq_lcextrap"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
