nextflow_process {

    name "Test Process PRESEQ_CCURVE"
    script "modules/nf-core/preseq/ccurve/main.nf"
    process "PRESEQ_CCURVE"
    config "tests/modules/nf-core/preseq_ccurve/nextflow.config"
    tag "preseq_ccurve"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
