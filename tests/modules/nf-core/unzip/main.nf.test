nextflow_process {

    name "Test Process UNZIP"
    script "modules/nf-core/unzip/main.nf"
    process "UNZIP"
    config "tests/modules/nf-core/unzip/nextflow.config"
    tag "unzip"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
