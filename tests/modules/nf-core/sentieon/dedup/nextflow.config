process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: 'DEDUP_.*' {
        ext.args4 = (params.rmdup) ? '--rmdup' : ''
    }

    withName: 'DEDUP_REMOVE' {
        ext.args4 = '--rmdup'
    }

}
