process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

sentieon {
  encryption_key_base64 = secrets.SENTIEON_ENCRYPTION_KEY_BASE64
  auth_mech_base64 = secrets.SENTIEON_AUTH_MECH_BASE64
  license_message_base64 = secrets.SENTIEON_LICENSE_MESSAGE_BASE64
}

}
