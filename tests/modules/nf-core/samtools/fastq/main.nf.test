nextflow_process {

    name "Test Process SAMTOOLS_FASTQ"
    script "modules/nf-core/samtools/fastq/main.nf"
    process "SAMTOOLS_FASTQ"
    config "tests/modules/nf-core/samtools_fastq/nextflow.config"
    tag "samtools_fastq"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
