nextflow_process {

    name "Test Process SAMTOOLS_FLAGSTAT"
    script "modules/nf-core/samtools/flagstat/main.nf"
    process "SAMTOOLS_FLAGSTAT"
    config "tests/modules/nf-core/samtools_flagstat/nextflow.config"
    tag "samtools_flagstat"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
