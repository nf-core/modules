nextflow_process {

    name "Test Process SAMTOOLS_COLLATE"
    script "modules/nf-core/samtools/collate/main.nf"
    process "SAMTOOLS_COLLATE"
    config "tests/modules/nf-core/samtools_collate/nextflow.config"
    tag "samtools_collate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
