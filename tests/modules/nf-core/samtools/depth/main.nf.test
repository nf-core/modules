nextflow_process {

    name "Test Process SAMTOOLS_DEPTH"
    script "modules/nf-core/samtools/depth/main.nf"
    process "SAMTOOLS_DEPTH"
    config "tests/modules/nf-core/samtools_depth/nextflow.config"
    tag "samtools_depth"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
