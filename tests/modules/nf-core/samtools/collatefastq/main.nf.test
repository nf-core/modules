nextflow_process {

    name "Test Process SAMTOOLS_COLLATEFASTQ"
    script "modules/nf-core/samtools/collatefastq/main.nf"
    process "SAMTOOLS_COLLATEFASTQ"
    config "tests/modules/nf-core/samtools_collatefastq/nextflow.config"
    tag "samtools_collatefastq"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
