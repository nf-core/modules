nextflow_process {

    name "Test Process SAMTOOLS_FIXMATE"
    script "modules/nf-core/samtools/fixmate/main.nf"
    process "SAMTOOLS_FIXMATE"
    config "tests/modules/nf-core/samtools_fixmate/nextflow.config"
    tag "samtools_fixmate"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
