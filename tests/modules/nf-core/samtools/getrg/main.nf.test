nextflow_process {

    name "Test Process SAMTOOLS_GETRG"
    script "modules/nf-core/samtools/getrg/main.nf"
    process "SAMTOOLS_GETRG"
    config "tests/modules/nf-core/samtools_getrg/nextflow.config"
    tag "samtools_getrg"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
