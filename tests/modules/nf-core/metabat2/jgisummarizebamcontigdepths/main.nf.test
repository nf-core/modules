nextflow_process {

    name "Test Process METABAT2_JGISUMMARIZEBAMCONTIGDEPTHS"
    script "modules/nf-core/metabat2/jgisummarizebamcontigdepths/main.nf"
    process "METABAT2_JGISUMMARIZEBAMCONTIGDEPTHS"
    config "tests/modules/nf-core/metabat2_jgisummarizebamcontigdepths/nextflow.config"
    tag "metabat2_jgisummarizebamcontigdepths"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
