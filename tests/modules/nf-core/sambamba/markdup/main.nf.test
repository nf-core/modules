nextflow_process {

    name "Test Process SAMBAMBA_MARKDUP"
    script "modules/nf-core/sambamba/markdup/main.nf"
    process "SAMBAMBA_MARKDUP"
    config "tests/modules/nf-core/sambamba_markdup/nextflow.config"
    tag "sambamba_markdup"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
