nextflow_process {

    name "Test Process HAPLOCHECK"
    script "modules/nf-core/haplocheck/main.nf"
    process "HAPLOCHECK"
    config "tests/modules/nf-core/haplocheck/nextflow.config"
    tag "haplocheck"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
