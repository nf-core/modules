nextflow_process {

    name "Test Process PICARD_ADDORREPLACEREADGROUPS"
    script "modules/nf-core/picard/addorreplacereadgroups/main.nf"
    process "PICARD_ADDORREPLACEREADGROUPS"
    config "tests/modules/nf-core/picard_addorreplacereadgroups/nextflow.config"
    tag "picard_addorreplacereadgroups"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
