nextflow_process {

    name "Test Process PICARD_COLLECTMULTIPLEMETRICS"
    script "modules/nf-core/picard/collectmultiplemetrics/main.nf"
    process "PICARD_COLLECTMULTIPLEMETRICS"
    config "tests/modules/nf-core/picard_collectmultiplemetrics/nextflow.config"
    tag "picard_collectmultiplemetrics"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
