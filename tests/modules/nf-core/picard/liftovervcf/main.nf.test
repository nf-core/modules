nextflow_process {

    name "Test Process PICARD_LIFTOVERVCF"
    script "modules/nf-core/picard/liftovervcf/main.nf"
    process "PICARD_LIFTOVERVCF"
    config "tests/modules/nf-core/picard_liftovervcf/nextflow.config"
    tag "picard_liftovervcf"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
