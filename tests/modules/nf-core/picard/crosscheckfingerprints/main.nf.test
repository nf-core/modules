nextflow_process {

    name "Test Process PICARD_CROSSCHECKFINGERPRINTS"
    script "modules/nf-core/picard/crosscheckfingerprints/main.nf"
    process "PICARD_CROSSCHECKFINGERPRINTS"
    config "tests/modules/nf-core/picard_crosscheckfingerprints/nextflow.config"
    tag "picard_crosscheckfingerprints"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
