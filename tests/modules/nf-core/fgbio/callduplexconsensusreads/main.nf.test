nextflow_process {

    name "Test Process FGBIO_CALLDUPLEXCONSENSUSREADS"
    script "modules/nf-core/fgbio/callduplexconsensusreads/main.nf"
    process "FGBIO_CALLDUPLEXCONSENSUSREADS"
    config "tests/modules/nf-core/fgbio_callduplexconsensusreads/nextflow.config"
    tag "fgbio_callduplexconsensusreads"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
