nextflow_process {

    name "Test Process FGBIO_ZIPPERBAMS"
    script "modules/nf-core/fgbio/zipperbams/main.nf"
    process "FGBIO_ZIPPERBAMS"
    config "tests/modules/nf-core/fgbio_zipperbams/nextflow.config"
    tag "fgbio_zipperbams"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
