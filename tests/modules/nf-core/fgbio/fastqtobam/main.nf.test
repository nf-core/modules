nextflow_process {

    name "Test Process FGBIO_FASTQTOBAM"
    script "modules/nf-core/fgbio/fastqtobam/main.nf"
    process "FGBIO_FASTQTOBAM"
    config "tests/modules/nf-core/fgbio_fastqtobam/nextflow.config"
    tag "fgbio_fastqtobam"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
