nextflow_process {

    name "Test Process BEAGLE5_BEAGLE"
    script "modules/nf-core/beagle5/beagle/main.nf"
    process "BEAGLE5_BEAGLE"
    config "tests/modules/nf-core/beagle5_beagle/nextflow.config"
    tag "beagle5_beagle"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
