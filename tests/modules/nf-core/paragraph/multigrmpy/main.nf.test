nextflow_process {

    name "Test Process PARAGRAPH_MULTIGRMPY"
    script "modules/nf-core/paragraph/multigrmpy/main.nf"
    process "PARAGRAPH_MULTIGRMPY"
    config "tests/modules/nf-core/paragraph_multigrmpy/nextflow.config"
    tag "paragraph_multigrmpy"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
