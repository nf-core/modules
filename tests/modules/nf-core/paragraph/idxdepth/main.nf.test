nextflow_process {

    name "Test Process PARAGRAPH_IDXDEPTH"
    script "modules/nf-core/paragraph/idxdepth/main.nf"
    process "PARAGRAPH_IDXDEPTH"
    config "tests/modules/nf-core/paragraph_idxdepth/nextflow.config"
    tag "paragraph_idxdepth"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
