nextflow_process {

    name "Test Process PARAGRAPH_VCF2PARAGRAPH"
    script "modules/nf-core/paragraph/vcf2paragraph/main.nf"
    process "PARAGRAPH_VCF2PARAGRAPH"
    config "tests/modules/nf-core/paragraph_vcf2paragraph/nextflow.config"
    tag "paragraph_vcf2paragraph"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
