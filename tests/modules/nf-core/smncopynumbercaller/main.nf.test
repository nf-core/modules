nextflow_process {

    name "Test Process SMNCOPYNUMBERCALLER"
    script "modules/nf-core/smncopynumbercaller/main.nf"
    process "SMNCOPYNUMBERCALLER"
    config "tests/modules/nf-core/smncopynumbercaller/nextflow.config"
    tag "smncopynumbercaller"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
