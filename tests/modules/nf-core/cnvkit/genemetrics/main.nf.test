nextflow_process {

    name "Test Process CNVKIT_GENEMETRICS"
    script "modules/nf-core/cnvkit/genemetrics/main.nf"
    process "CNVKIT_GENEMETRICS"
    config "tests/modules/nf-core/cnvkit_genemetrics/nextflow.config"
    tag "cnvkit_genemetrics"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
