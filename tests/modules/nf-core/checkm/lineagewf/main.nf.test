nextflow_process {

    name "Test Process CHECKM_LINEAGEWF"
    script "modules/nf-core/checkm/lineagewf/main.nf"
    process "CHECKM_LINEAGEWF"
    config "tests/modules/nf-core/checkm_lineagewf/nextflow.config"
    tag "checkm_lineagewf"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
