nextflow_process {

    name "Test Process PLINK_VCF"
    script "modules/nf-core/plink/vcf/main.nf"
    process "PLINK_VCF"
    config "tests/modules/nf-core/plink_vcf/nextflow.config"
    tag "plink_vcf"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
