nextflow_process {

    name "Test Process CAT_FASTQ"
    script "modules/nf-core/cat/fastq/main.nf"
    process "CAT_FASTQ"
    config "tests/modules/nf-core/cat_fastq/nextflow.config"
    tag "cat_fastq"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
