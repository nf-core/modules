nextflow_process {

    name "Test Process IQTREE"
    script "modules/nf-core/iqtree/main.nf"
    process "IQTREE"
    config "tests/modules/nf-core/iqtree/nextflow.config"
    tag "iqtree"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
