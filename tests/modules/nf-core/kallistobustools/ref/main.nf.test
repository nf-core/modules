nextflow_process {

    name "Test Process KALLISTOBUSTOOLS_REF"
    script "modules/nf-core/kallistobustools/ref/main.nf"
    process "KALLISTOBUSTOOLS_REF"
    config "tests/modules/nf-core/kallistobustools_ref/nextflow.config"
    tag "kallistobustools_ref"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
