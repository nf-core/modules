nextflow_process {

    name "Test Process NCBIGENOMEDOWNLOAD"
    script "modules/nf-core/ncbigenomedownload/main.nf"
    process "NCBIGENOMEDOWNLOAD"
    config "tests/modules/nf-core/ncbigenomedownload/nextflow.config"
    tag "ncbigenomedownload"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
