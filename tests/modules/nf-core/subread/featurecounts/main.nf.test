nextflow_process {

    name "Test Process SUBREAD_FEATURECOUNTS"
    script "modules/nf-core/subread/featurecounts/main.nf"
    process "SUBREAD_FEATURECOUNTS"
    config "tests/modules/nf-core/subread_featurecounts/nextflow.config"
    tag "subread_featurecounts"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
