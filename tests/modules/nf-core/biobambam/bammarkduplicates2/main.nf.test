nextflow_process {

    name "Test Process BIOBAMBAM_BAMMARKDUPLICATES2"
    script "modules/nf-core/biobambam/bammarkduplicates2/main.nf"
    process "BIOBAMBAM_BAMMARKDUPLICATES2"
    config "tests/modules/nf-core/biobambam_bammarkduplicates2/nextflow.config"
    tag "biobambam_bammarkduplicates2"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
