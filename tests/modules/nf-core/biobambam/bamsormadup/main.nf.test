nextflow_process {

    name "Test Process BIOBAMBAM_BAMSORMADUP"
    script "modules/nf-core/biobambam/bamsormadup/main.nf"
    process "BIOBAMBAM_BAMSORMADUP"
    config "tests/modules/nf-core/biobambam_bamsormadup/nextflow.config"
    tag "biobambam_bamsormadup"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            // Assert that the process ran successfully
            assert process.success
            // Assert that the process produced the expected output
            assert snapshot(path(params.outdir).list()).match()
        }

    }

}
