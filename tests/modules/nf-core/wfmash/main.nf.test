nextflow_process {

    name "Test Process WFMASH"
    script "modules/nf-core/wfmash/main.nf"
    process "WFMASH"
    config "tests/modules/nf-core/wfmash/nextflow.config"
    tag "wfmash"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
