nextflow_process {

    name "Test Process BBMAP_PILEUP"
    script "modules/nf-core/bbmap/pileup/main.nf"
    process "BBMAP_PILEUP"
    config "tests/modules/nf-core/bbmap/pileup/nextflow.config"
    tag "bbmap_pileup"

    test("Sarscov2 Illumina PE BAM") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                input[0] = [
                            [ id:'test', single_end:false ], // meta map
                            file(params.test_data['sarscov2']['illumina']['test_paired_end_bam'], checkIfExists: true)
                           ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
