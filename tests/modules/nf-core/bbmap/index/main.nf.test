nextflow_process {

    name "Test Process BBMAP_INDEX"
    script "modules/nf-core/bbmap/index/main.nf"
    process "BBMAP_INDEX"
    config "tests/modules/nf-core/bbmap_index/nextflow.config"
    tag "bbmap_index"

    test("Should run without failures") {

        when {
            params {
                outdir = "$outputDir"
            }
            process {
                """
                // define inputs of the process here. Example:
                // input[0] = file("test-file.txt")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(path(params.outdir).list()).match() }
            )
        }

    }

}
