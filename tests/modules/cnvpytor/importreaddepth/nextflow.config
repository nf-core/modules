process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: CNVPYTOR_IMPORTREADDEPTH {
        ext.args = '' //-chrom 1 2 3 ... could be added if desired.
    }
}
