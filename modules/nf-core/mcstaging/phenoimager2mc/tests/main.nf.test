// TODO nf-core: Once you have added the required tests, please run the following command to build this file:
// nf-core modules test mcstaging/phenoimager2mc
nextflow_process {

    name "Test Process MCSTAGING_PHENOIMAGER2MC"
    script "../main.nf"
    process "MCSTAGING_PHENOIMAGER2MC"
        config "./nextflow.config"

    tag "modules"
    tag "modules_nfcore"
    tag "mcstaging"
    tag "mcstaging/phenoimager2mc"

    test("Should run without failures") {
        when {
            process {
                """
                // define inputs of the process here. Example:
                input[0] = [
                    [ id:'test' ],
                    file(params.test_data['imaging']['staging']['folder'], checkIfExists: true).toString()
                ]
                """
            }
        }
        then {
            assertAll (
                { assert process.success },
                { assert process.out.tif },
                { assert snapshot(process.out.versions).match("versions") }
            )
        }
    }
}