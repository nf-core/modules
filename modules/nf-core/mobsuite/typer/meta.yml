name: "mobsuite_typer"
description: Bacterial plasmid typing and mobility prediction
keywords:
  - bacteria
  - plasmid
  - typing
  - genomics
tools:
  - "mobsuite":
      description: "MOB-suite is a set of tools for finding, typing and reconstruction
        of plasmids from draft and complete genome assemblies."
      homepage: "https://github.com/phac-nml/mob-suite/blob/master/README.md"
      documentation: "https://github.com/phac-nml/mob-suite/blob/master/README.md"
      tool_dev_url: "https://github.com/phac-nml/mob-suite"
      doi: "10.1099/mgen.0.000435"
      licence:
        - "Apache-2.0"
      identifier: biotools:mob_suite
input:
  - - meta:
        type: map
        description: |
          Groovy Map containing sample information
          e.g. `[ id:'sample1' ]`
    - fasta:
        type: file
        description: A bacterial genome assembly in FASTA format
        pattern: "*.{fasta,fa,fna}"
        ontologies:
          - edam: "http://edamontology.org/format_1929" # FASTA
  - db:
      type: directory
      description: Optional local copy of MOB-suite database directory
      pattern: "databases/"
  - plasmid_mash_db:
      type: file
      description: |
        Optional Companion Mash database of reference database.
        If not supplied, default internal `ncbi_plasmid_full_seqs.fas.msh` is used.
      pattern: "*.msh"
      ontologies:
        - edam: http://edamontology.org/format_3911 # msh
  - plasmid_meta_txt:
      type: file
      description: |
        Optional MOB-cluster plasmid cluster formatted file matched to the reference
        plasmid db. 
        If not supplied, default internal `clusters.txt` is used.
      pattern: "*.txt"
      ontologies:
        - edam: "http://edamontology.org/format_3671" # Text
  - plasmid_replicons_fas:
      type: file
      description: |
        Optional nucleotide FASTA file of plasmid replicons
        If not supplied, default internal `rep.dna.fas` is used.
      pattern: "*.{fasta,fna,fa,fas}"
      ontologies:
        - edam: "http://edamontology.org/format_1929" # FASTA
  - repetitive_mask_fas:
      type: file
      description: |
        Optional nucleotide FASTA file of known repetitive elements
        If not supplied, default internal `repetitive.dna.fas` is used.
      pattern: "*.{fasta,fna,fa,fas}"
      ontologies:
        - edam: "http://edamontology.org/format_1929" # FASTA
  - plasmid_mob_faa:
      type: file
      description: |
        Optional protein FASTA file of plasmid mobility proteins
        If not supplied, default internal `mob.proteins.faa` is used.
      pattern: "*.{fasta,faa,fa,fas}"
      ontologies:
        - edam: "http://edamontology.org/format_1929" # FASTA
  - plasmid_mpf_faa:
      type: file
      description: |
        Optional protein FASTA file of plasmid mating pair formation proteins
        If not supplied, default internal `mpf.proteins.faa` is used.
      pattern: "*.{fasta,faa,fa,fas}"
      ontologies:
        - edam: "http://edamontology.org/format_1929" # FASTA
  - plasmid_orit_fas:
      type: file
      description: |
        Optional nucleotide FASTA file of known plasmid oriT dna sequences
        If not supplied, default internal `orit.fas` is used.
      pattern: "*.{fasta,fna,fa,fas}"
      ontologies:
        - edam: "http://edamontology.org/format_1929" # FASTA
  - generate_biomarker_report:
      type: boolean
      description: Whether to generate a biomarker report file
  - generate_mge_report:
      type: boolean
      description: Whether to generate a mobile genetic element (MGE) report file
output:
  report:
    - - meta:
          type: map
          description: |
            Groovy Map containing sample information
            e.g. `[ id:'sample1' ]`
      - "*.txt":
          type: file
          description: MOB-suite typer report file
          pattern: "*.txt"
          ontologies:
            - edam: "http://edamontology.org/format_3671" # Text
  biomarker_report:
    - - meta:
          type: map
          description: |
            Groovy Map containing sample information
            e.g. `[ id:'sample1' ]`
      - "*_biomarker_report.txt":
          type: file
          description: MOB-suite biomarker report file
          pattern: "*_biomarker_report.txt"
          ontologies:
            - edam: "http://edamontology.org/format_3671" # Text
  mge_report:
    - - meta:
          type: map
          description: |
            Groovy Map containing sample information
            e.g. `[ id:'sample1' ]`
      - "*_mge_report.txt":
          type: file
          description: MOB-suite mobile genetic element (MGE) report file
          pattern: "*_mge_report.txt"
          ontologies:
            - edam: "http://edamontology.org/format_3671" # Text
  versions_mobsuite:
    - - ${task.process}:
          type: string
          description: The name of the process
      - mobsuite:
          type: string
          description: The name of the tool
      - mob_typer --version | sed 's/mob_typer //g':
          type: eval
          description: The expression to obtain the version of the tool
topics:
  versions:
    - - ${task.process}:
          type: string
          description: The name of the process
      - mobsuite:
          type: string
          description: The name of the tool
      - mob_typer --version | sed 's/mob_typer //g':
          type: eval
          description: The expression to obtain the version of the tool
authors:
  - "@jfy133"
maintainers:
  - "@jfy133"
