process {
    withName: 'PARABRICKS_STARFUSION' {
        memory = '15 GB'
    }
    withName: 'PARABRICKS_RNAFQ2BAM' {
        ext.args = '--low-memory --read-files-command zcat --out-chim-type Junctions'
        memory = '15 GB'
    }
}

docker.runOptions = '-u $(id -u):$(id -g) --gpus device=0'
