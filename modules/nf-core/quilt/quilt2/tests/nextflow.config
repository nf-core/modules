process {
    // More than 1 cpu may lead to different md5sum
    resourceLimits = [
        cpus: 1
    ]
    withName: SAMTOOLS_VIEW {
        ext.args = { "${params.samtools_args}" }
    }
    withName: QUILT_QUILT2 {
        ext.args = { "${params.quilt_args}" }
    }
}
