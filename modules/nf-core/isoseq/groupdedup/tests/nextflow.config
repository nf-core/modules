process {

    // isoseq groupdedup's POA consensus step is non-deterministic across threads
    // (confirmed for the same isoseq cluster2 algorithm family); pin to a single
    // thread so the test snapshot is reproducible.
    cpus = 1

}
