process {
    ext.prefix = { "${meta.id}" }
    ext.args   = [
        "--sample_id_col \"sample\"",
        "--minimum_samples 1.0",
        "--minimum_abundance 10",
        "--minimum_proportion_not_na \"0.5\""
    ].join(' ').trim()
}
