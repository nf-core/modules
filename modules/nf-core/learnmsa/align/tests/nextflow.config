process {

    // The container is overwritten because the original container is 13GB
    // and it cannot be currently run on the runners.
    // Tests are run in stub using this container instead.
    withName: LEARNMSA_ALIGN{
        container = "nf-core/ubuntu:22.04"
    }

}
