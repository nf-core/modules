process {
    withName: 'BCLCONVERT' {
        ext.args = {[
            meta.lane ? "--bcl-only-lane ${meta.lane}" : "",
            meta.tile ? "--tiles ${meta.tile}": "",
            "--force"
            ].join(" ").trim()}
    }
}
