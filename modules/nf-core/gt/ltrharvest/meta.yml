---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/meta-schema.json
name: "gt_ltrharvest"
description: Predicts LTR retrotransposons using GenomeTools gt-ltrharvest utility
keywords:
  - genomics
  - genome
  - annotation
  - repeat
  - transposons
  - retrotransposons
tools:
  - "gt":
      description: "The GenomeTools genome analysis system"
      homepage: "https://genometools.org/index.html"
      documentation: "https://genometools.org/documentation.html"
      tool_dev_url: "https://github.com/genometools/genometools"
      doi: "10.1109/TCBB.2013.68"
      licence: ["ISC"]
input:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. `[ id:'sample1' ]`
  - index:
      type: directory
      description: Folder containing the suffixerator index files
      pattern: "suffixerator"
output:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. `[ id:'sample1' ]`
  - tabout:
      type: file
      description: Old tabular output by default or when `-tabout yes` argument is present
      pattern: "*.tabout"
  - gff3:
      type: file
      description: GFF3 output when `-tabout no` argument is present
      pattern: "*.gff3"
  - fasta:
      type: file
      description: FASTA output when `-out out.fasta` argument is present
      pattern: "out.fasta"
  - inner_fasta:
      type: file
      description: FASTA output for inner regions when `-outinner inner.fasta` argument is present
      pattern: "inner.fasta"
  - versions:
      type: file
      description: File containing software versions
      pattern: "versions.yml"
authors:
  - "@GallVp"
maintainers:
  - "@GallVp"
