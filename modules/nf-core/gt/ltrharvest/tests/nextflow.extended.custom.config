process {

    withName: GT_SUFFIXERATOR {
        ext.args = '-suf -lcp'
        // GT_LTRHARVEST requires -suf, -lcp
    }

    withName: GT_LTRHARVEST {
        ext.args =  '-tabout no -out custom.custom2.fa -outinner custom.custom2.custom3_custom4.fsa'
    }
}
