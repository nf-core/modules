process {
    withName: DEEPVARIANT_RUNDEEPVARIANT {
        ext.args = {
            [
                params.module_args,
                '--postprocess_variants_extra_args="cpus=2"'
            ].findAll { it }.join(' ')
        }
        ext.prefix = { "${meta.id}_out" }
    }
}
