process {

    withName: DELLY_PRECALL {
        ext.prefix = { "${meta.id}.for_genotyping" }
        ext.suffix = 'vcf'
    }

    withName: DELLY_CALL {
        ext.suffix = { params.suffix ?: '' }
    }
}