process {
    withName: CIRCULARMAPPER_REALIGNSAMFILE {
        ext.args = { [
            '-f true',
            '-x false',
        ].join(' ') }
        ext.prefix = { params.elong ? "${params.elong}" : '500' }
    }
}
