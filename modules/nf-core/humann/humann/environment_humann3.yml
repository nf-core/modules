---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
# Used when process is aliased as HUMANN3
# NOTE: base.nf uses condaMap dynamically; this file is for reference only.
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::humann=3.6.1
