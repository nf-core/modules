process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: ANGSD_GL {
        // -doMajorMinor is needed for -doGlf 3
        ext.args = '-GL 3 -doGlf 3 -doMajorMinor 1'
    }

}
