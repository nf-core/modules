process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

    withName: BCFTOOLS_MERGE {
        ext.args = {"--force-samples --no-version -m none --output-type z --write-index=tbi --gvcf $fasta"}
    }
}
