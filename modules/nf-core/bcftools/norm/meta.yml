name: bcftools_norm
description: Normalize VCF file
keywords:
  - normalize
  - norm
  - variant calling
  - VCF
tools:
  - norm:
      description: |
        Normalize VCF files.
      homepage: http://samtools.github.io/bcftools/bcftools.html
      documentation: http://www.htslib.org/doc/bcftools.html
      doi: 10.1093/bioinformatics/btp352
      licence: ["MIT"]
input:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. [ id:'test', single_end:false ]
  - vcf:
      type: file
      description: |
        The vcf file to be normalized
        e.g. 'file1.vcf'
      pattern: "*.{vcf,vcf.gz}"
  - tbi:
      type: file
      description: |
        An optional index of the VCF file (for when the VCF is compressed)
      pattern: "*.vcf.gz.tbi"
  - fasta:
      type: file
      description: FASTA reference file
      pattern: "*.{fasta,fa}"
output:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. [ id:'test', single_end:false ]
  - vcfgz:
      type: file
      description: Compressed VCF normalized output file
      pattern: "*.vcf.gz"
  - vcf:
      type: file
      description: Uncompressed VCF normalized output file
      pattern: "*.vcf"
  - bcfgz:
      type: file
      description: Compressed BCF normalized output file
      pattern: "*.bcf.gz"
  - bcf:
      type: file
      description: Uncompressed BCF normalized output file
      pattern: "*.bcf"
  - versions:
      type: file
      description: File containing software versions
      pattern: "versions.yml"
authors:
  - "@abhi18av"
