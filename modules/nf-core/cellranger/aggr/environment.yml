---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
# Cell Ranger is not available via Conda. Use Docker or Singularity instead.
# The module will raise an error if run with -profile conda or -profile mamba.
channels:
  - conda-forge
  - bioconda
dependencies: []
