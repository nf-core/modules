process {
    withName: XENIUMRANGER_RESEGMENT {
        ext.args = '--expansion-distance=5'
        container = "dongze_he/xeniumranger:4.0"
    }
    containerOptions = '-u $(id -u):$(id -g)'
}

docker.enabled = true
