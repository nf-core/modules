process {
    withName: XENIUMRANGER_RELABEL {
        // container = "dongze_he/xeniumranger:4.0"
        cpus = 32
        memory = '120GB'
    }
    containerOptions = '-u $(id -u):$(id -g)'

}

docker.enabled = true
