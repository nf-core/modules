nextflow_process {


    name "Test Process HIFITRIMMER_FILTERBAM"
    script "../main.nf"
    process "HIFITRIMMER_FILTERBAM"

    tag "modules"
    tag "modules_nfcore"
    tag "hifitrimmer"
    tag "hifitrimmer/filterbam"

    config './nextflow.config'

    test("hifi_trimmer filter_bam test - fasta") {

        when {
            params {
                filter_bam_args    = ""
            }
            process {
                """
                input[0] = Channel.of(
                    [
                        [ id:'test' ], // meta map
                        file(params.modules_testdata_base_path + '/delete_me/hifitrimmer/test.bam', checkIfExists: true),
                        file(params.modules_testdata_base_path + '/delete_me/hifitrimmer/test.bed.gz', checkIfExists: true)
                    ]
                )
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                    process.out.filtered,
                    process.out.versions_hifitrimmer
                    ).match()
                }
            )
        }
    }

    test("hifi_trimmer filter_bam test - fastq") {

        when {
            params {
                filter_bam_args    = "-f"
            }
            process {
                """
                input[0] = Channel.of(
                    [
                        [ id:'test' ], // meta map
                        file(params.modules_testdata_base_path + '/delete_me/hifitrimmer/test.bam', checkIfExists: true),
                        file(params.modules_testdata_base_path + '/delete_me/hifitrimmer/test.bed.gz', checkIfExists: true)
                    ]
                )
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                    process.out.filtered,
                    process.out.versions_hifitrimmer
                    ).match()
                }
            )
        }
    }

    test("hifi_trimmer test - stub") {

        options "-stub"

        when {

            process {
                """
                input[0] = Channel.of(
                    [
                        [ id:'test' ], // meta map
                        file(params.modules_testdata_base_path + '/delete_me/hifitrimmer/test.bam', checkIfExists: true),
                        file(params.modules_testdata_base_path + '/delete_me/hifitrimmer/test.bed.gz', checkIfExists: true)
                    ]
                )
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() },
                { assert snapshot(process.out.versions_hifitrimmer).match("versions") }
            )
        }
    }
}
