process{
    withName: TABIX_BGZIP_1 {
        ext.prefix = {input.toString() - ".vcf.gz"}
    }
    withName: TABIX_BGZIP_2 {
        ext.prefix = {input.toString() - ".vcf.gz"}
    }
    withName: WITTYER {
        containerOptions = {
            workflow.containerEngine == 'singularity' || workflow.containerEngine == 'apptainer'
                ? "--home ${launchDir}"
                : null
        }
    }
}
