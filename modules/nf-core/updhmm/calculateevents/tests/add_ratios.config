process {
    withName: 'UPDHMM_CALCULATEEVENTS' {
        ext.args = '--verbose true --add_ratios true'
        cpus = 1
    }
    
    withName: 'BCFTOOLS_VIEW' {
        ext.args = '-i "COUNT(GT=\\"mis\\") == 0"'
    }
}