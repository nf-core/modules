// nf-core modules test traitar
nextflow_process {

    name "Test Process TRAITAR"
    script "../main.nf"
    process "TRAITAR"

    tag "modules"
    tag "modules_nfcore"
    tag "traitar"

    test("traitar - proteomes - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id:'test_sample' ],
                    file('https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/genomics/prokaryotes/bacteroides_fragilis/genome/genome.fna.gz', checkIfExists: false),
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

}
