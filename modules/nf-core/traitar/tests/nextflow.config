params {
    modules_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/'
    outdir = 'output'
    publish_dir_mode = 'copy'
}

process {
    ext.args = '--overwrite'
    ext.input_type = 'from_genes'
    // For stub tests, use null container to avoid pulling image
    // Real tests will use the Wave container from main.nf
}
