process {
    withName: POOLSNP {
        publishDir = [
            path: { "${params.outdir}/poolsnp" },
            mode: 'copy'
        ]
        ext.args = "max-cov=0.7 min-cov=4 min-count=4 min-freg=0.01 miss-frac=0.5 BS=1 all_sites=1"
    }
}