conda.enabled = false
docker.enabled = false
singularity.enabled = false

profiles {
    conda {
        conda.enabled = true
    }

    docker {
        docker.enabled = true
    }

    singularity {
        singularity.enabled = true
    }
}
