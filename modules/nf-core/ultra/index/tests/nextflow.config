process {
    withName: "GNU_SORT" {
        ext.args = params.gnu_sort_args
        ext.prefix = { "${meta.id}_sorted" }
    }

    withName: "ULTRA_INDEX" {
        ext.args = params.ultra_index_args
    }
}
