nextflow_process {

    name "Test Process STRINGTIE_STRINGTIE"
    script "../main.nf"
    process "STRINGTIE_STRINGTIE"
    tag "modules"
    tag "modules_nfcore"
    tag "stringtie"
    tag "stringtie/stringtie"

    test("sarscov2 [bam] - forward strandedness") {

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'forward' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            []
                        ]
                input[1] = []
                input[2] = ""
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["coverage_gtf", "abundance"])).match() }
            )
        }
    }

    test("sarscov2 [bam] - forward strandedness + reference annotation") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test', strandedness:'forward' ], // meta map
                    [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                    []
                ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = ""
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["coverage_gtf", "abundance"])).match() }
            )
        }
    }

    test("sarscov2 [bam] - reverse strandedness") {

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            []
                        ]
                input[1] = []
                input[2] = ""
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["coverage_gtf", "abundance"])).match() }
            )
        }
    }

    test("sarscov2 [bam] - reverse strandedness + reference annotation") {

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            []
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = ""
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["coverage_gtf", "abundance"])).match() }
            )
        }
    }

    test("sarscov2 [bam] - forward strandedness - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'forward' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            []
                        ]
                input[1] = []
                input[2] = "expression-estimation"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }
    }

    test("sarscov2 [bam] - forward strandedness + reference annotation - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id:'test', strandedness:'forward' ], // meta map
                    [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                    []
                ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "expression-estimation"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }
    }

    test("sarscov2 [bam] - reverse strandedness - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            []
                        ]
                input[1] = []
                input[2] = "expression-estimation"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }
    }

    test("sarscov2 [bam] - reverse strandedness + reference annotation - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            []
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "expression-estimation"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }
    }

    test("mix-read-assembly pacbio bam + reference annotation") {

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            [ file(params.modules_testdata_base_path + "genomics/homo_sapiens/pacbio/bam/test_hifi_aligned_assembly.bam", checkIfExists: true) ]
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "mix-reads-assembly"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["coverage_gtf", "abundance"])).match() }
            )
        }
    }

    test("mix-read-assembly pacbio bam + reference annotation - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [ file(params.modules_testdata_base_path + "genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam", checkIfExists: true) ],
                            [ file(params.modules_testdata_base_path + "genomics/homo_sapiens/pacbio/bam/test_hifi_aligned_assembly.bam", checkIfExists: true) ]
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "mix-reads-assembly"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }
    }

    test("long-read-assembly pacbio bam + reference annotation") {

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [],
                            [ file(params.modules_testdata_base_path + "genomics/homo_sapiens/pacbio/bam/test_hifi_aligned_assembly.bam", checkIfExists: true) ]
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "long-reads-assembly"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["coverage_gtf", "abundance"])).match() }
            )
        }
    }

    test("long-read-assembly pacbio bam + reference annotation - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [],
                            [ file(params.modules_testdata_base_path + "genomics/homo_sapiens/pacbio/bam/test_hifi_aligned_assembly.bam", checkIfExists: true) ]
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "long-reads-assembly"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }
    }

    test("long-read-assembly ont bam + reference annotation") {

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [],
                            [ file(params.modules_testdata_base_path + "genomics/homo_sapiens/nanopore/bam/test.sorted.bam", checkIfExists: true) ]
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "long-reads-assembly"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["coverage_gtf", "abundance"])).match() }
            )
        }
    }

    test("long-read-assembly ont bam + reference annotation - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                            [ id:'test', strandedness:'reverse' ], // meta map
                            [],
                            [ file(params.modules_testdata_base_path + "genomics/homo_sapiens/nanopore/bam/test.sorted.bam", checkIfExists: true) ]
                        ]
                input[1] = file(params.modules_testdata_base_path + "genomics/sarscov2/genome/genome.gtf", checkIfExists: true)
                input[2] = "long-reads-assembly"
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }
    }
}
