process {
    withName: 'MODKIT_ENTROPY' {
        // --header makes the genome-wide BED and the per-window bedgraph
        // self-describing, so the tests can assert on the column names.
        ext.args = '--cpg --header'
    }
}
