process {
    withName: 'MODKIT_LOCALIZE_PLOT' {
        ext.args = params.module_args ?: ''
    }
}

// Prevent quay.io/ prefix — Wave URI is not on quay.io
singularity.registry = ''
docker.registry      = ''
