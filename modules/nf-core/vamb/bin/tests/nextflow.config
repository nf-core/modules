process {
    withName: VAMB_BIN {
        ext.args = params.vamb_args
    }

    withName: MMSEQS_TAXONOMY {
        ext.args = params.mmseqs_taxonomy_args
    }

    withName: GAWK {
        ext.prefix = params.gawk_prefix
        ext.args2 = params.gawk_args2
    }
}
