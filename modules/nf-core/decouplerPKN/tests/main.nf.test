nextflow_process {

    name "Test Process GET_DECOUPLER_PKN"
    script "../main.nf"
    process "GET_DECOUPLER_PKN"

    tag "modules"
    tag "modules_nfcore"
    tag "decouplerPKN"

    test("decouplerPKN_test") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ],
                    'progeny',
                    'human'
                    ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

}
