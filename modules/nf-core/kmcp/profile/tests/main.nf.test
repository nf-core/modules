nextflow_process {

    name "Test Process KMCP_PROFILE"
    script "../main.nf"
    process "KMCP_PROFILE"
    config "./nextflow.config"

    tag "modules"
    tag "modules_nfcore"
    tag "kmcp"
    tag "kmcp/profile"
    tag "untar"
    tag "kmcp/compute"
    tag "kmcp/index"
    tag "kmcp/search"

    test("sarscov2 - fasta - strain") {

        setup {
            run("KMCP_COMPUTE") {
                script "../../../kmcp/compute/main.nf"
                process {
                    """
                    input[0] = [
                                    [id: 'test'],
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta', checkIfExists: true)
                                ]
                    """
                }
            }

            run("KMCP_INDEX") {
                script "../../../kmcp/index/main.nf"
                process {
                    """
                    input[0] = KMCP_COMPUTE.out.outdir
                    input[1] = [[],[]]
                    input[2] = [[],[]]
                    """
                }
            }

            run("KMCP_SEARCH") {
                script "../../../kmcp/search/main.nf"
                process {
                    """
                    input[0] = [
                                    [id:'test', single_end:true ], // meta map
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_1.fastq.gz', checkIfExists: true)
                                ]
                    input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                    """
                }
            }
        }

        when {
            process {
                """
                input[0] = KMCP_SEARCH.out.result
                input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                input[2] = 'strain' 
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                        process.out,
                    ).match()
                }
            )
        }

    }

    test("sarscov2 - fasta - species - missing taxonomy") {

        setup {
            run("KMCP_COMPUTE") {
                script "../../../kmcp/compute/main.nf"
                process {
                    """
                    input[0] = [
                                    [id: 'test'],
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta', checkIfExists: true)
                                ]
                    """
                }
            }

            run("KMCP_INDEX") {
                script "../../../kmcp/index/main.nf"
                process {
                    """
                    input[0] = KMCP_COMPUTE.out.outdir
                    input[1] = [[],[]]
                    input[2] = [[],[]]
                    """
                }
            }

            run("KMCP_SEARCH") {
                script "../../../kmcp/search/main.nf"
                process {
                    """
                    input[0] = [
                                    [id:'test', single_end:true ], // meta map
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_1.fastq.gz', checkIfExists: true)
                                ]
                    input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                    """
                }
            }
        }

        when {
            process {
                """
                input[0] = KMCP_SEARCH.out.result
                input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                input[2] = 'species' 
                """
            }
        }

        then {
            assertAll(
                { assert process.failed },
                { assert process.errorReport.contains("ERROR: --level 'species' requires taxdump files to be provided in the database directory.") }
            )
        }

    }

    test("sarscov2 - fasta - species - with taxonomy") {

        setup {
            run("KMCP_COMPUTE") {
                script "../../../kmcp/compute/main.nf"
                process {
                    """
                    input[0] = [
                                    [id: 'test'],
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta', checkIfExists: true)
                                ]
                    """
                }
            }

            run("KMCP_INDEX") {
                script "../../../kmcp/index/main.nf"
                process {
                    """
                    input[0] = KMCP_COMPUTE.out.outdir
                    input[1] = [
                        [id: 'taxdmp'], 
                        [
                            file(params.modules_testdata_base_path + 'genomics/sarscov2/metagenome/nodes.dmp', checkIfExists: true),
                            file(params.modules_testdata_base_path + 'genomics/sarscov2/metagenome/names.dmp', checkIfExists: true)
                        ]
                    ]
                    input[2] = channel.of("MT192765.1\t2697049").collectFile(name: "genome2taxid.map", newLine: true).map{ mapfile -> [[id: 'genome2taxid'], mapfile]}
                    """
                }
            }

            run("KMCP_SEARCH") {
                script "../../../kmcp/search/main.nf"
                process {
                    """
                    input[0] = [
                                    [id:'test', single_end:true ], // meta map
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_1.fastq.gz', checkIfExists: true)
                                ]
                    input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                    """
                }
            }
        }

        when {
            process {
                """
                input[0] = KMCP_SEARCH.out.result
                input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                input[2] = 'species'
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                        process.out,
                    ).match()
                }
            )
        }

    }

    test("sarscov2 - fasta - strain - stub") {

        options "-stub"

        setup {
            run("KMCP_COMPUTE") {
                script "../../../kmcp/compute/main.nf"
                process {
                    """
                    input[0] = [
                                    [id: 'test'],
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta', checkIfExists: true)
                                ]
                    """
                }
            }

            run("KMCP_INDEX") {
                script "../../../kmcp/index/main.nf"
                process {
                    """
                    input[0] = KMCP_COMPUTE.out.outdir
                    input[1] = [[],[]]
                    input[2] = [[],[]]
                    """
                }
            }

            run("KMCP_SEARCH") {
                script "../../../kmcp/search/main.nf"
                process {
                    """
                    input[0] = [
                                    [id:'test', single_end:true ], // meta map
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_1.fastq.gz', checkIfExists: true)
                                ]
                    input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                    """
                }
            }
        }

        when {
            process {
                """
                input[0] = KMCP_SEARCH.out.result
                input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                input[2] = 'strain' 
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

    test("sarscov2 - fasta - wrong level - stub") {

        options "-stub"

        setup {
            run("KMCP_COMPUTE") {
                script "../../../kmcp/compute/main.nf"
                process {
                    """
                    input[0] = [
                                    [id: 'test'],
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta', checkIfExists: true)
                                ]
                    """
                }
            }

            run("KMCP_INDEX") {
                script "../../../kmcp/index/main.nf"
                process {
                    """
                    input[0] = KMCP_COMPUTE.out.outdir
                    input[1] = [[],[]]
                    input[2] = [[],[]]
                    """
                }
            }

            run("KMCP_SEARCH") {
                script "../../../kmcp/search/main.nf"
                process {
                    """
                    input[0] = [
                                    [id:'test', single_end:true ], // meta map
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_1.fastq.gz', checkIfExists: true)
                                ]
                    input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                    """
                }
            }
        }

        when {
            process {
                """
                input[0] = KMCP_SEARCH.out.result
                input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                input[2] = 'broken' 
                """
            }
        }

        then {
            assertAll(
                { assert process.failed },
                { assert process.errorReport.contains("[KMCP_PROFILE] ERROR: --level must be one of 'species', 'strain' or 'assembly") }
            )
        }

    }

    test("sarscov2 - fasta - species - with taxonomy - stub") {

        options "-stub"

        setup {
            run("KMCP_COMPUTE") {
                script "../../../kmcp/compute/main.nf"
                process {
                    """
                    input[0] = [
                                    [id: 'test'],
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/genome/genome.fasta', checkIfExists: true)
                                ]
                    """
                }
            }

            run("KMCP_INDEX") {
                script "../../../kmcp/index/main.nf"
                process {
                    """
                    input[0] = KMCP_COMPUTE.out.outdir
                    input[1] = [
                        [id: 'taxdmp'], 
                        [
                            file(params.modules_testdata_base_path + 'genomics/sarscov2/metagenome/nodes.dmp', checkIfExists: true),
                            file(params.modules_testdata_base_path + 'genomics/sarscov2/metagenome/names.dmp', checkIfExists: true)
                        ]
                    ]
                    input[2] = channel.of("genome\t2697049").collectFile(name: "genome2taxid.map", newLine: true).map{ mapfile -> [[id: 'genome2taxid'], mapfile]}
                    """
                }
            }

            run("KMCP_SEARCH") {
                script "../../../kmcp/search/main.nf"
                process {
                    """
                    input[0] = [
                                    [id:'test', single_end:true ], // meta map
                                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/fastq/test_1.fastq.gz', checkIfExists: true)
                                ]
                    input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                    """
                }
            }
        }

        when {
            process {
                """
                input[0] = KMCP_SEARCH.out.result
                input[1] = KMCP_INDEX.out.kmcp.map{it[1]}
                input[2] = 'species' 
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                        process.out,
                    ).match()
                }
            )
        }
    }

}