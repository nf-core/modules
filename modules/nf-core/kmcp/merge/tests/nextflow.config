process {
    withName: KMCP_COMPUTE {
        ext.prefix = params.kmcp_compute_prefix
    }
    withName: KMCP_INDEX {
        ext.prefix = params.kmcp_index_prefix
    }
    withName: KMCP_SEARCH {
        ext.prefix = params.kmcp_search_prefix
    }
}
