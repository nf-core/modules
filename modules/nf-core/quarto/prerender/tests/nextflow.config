// Explicitly set number of CPUs so that `params.yml` is stable in CI testing,
// which affects the snapshot of `params.yml` and the final rendered markdown.
process {
    withName: QUARTO_PRERENDER {
        cpus = 1
    }
}
