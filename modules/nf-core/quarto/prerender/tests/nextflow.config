// Pinned: task.cpus is embedded in params.yml and printed in the rendered
// markdown, so leaving it unset breaks snapshot reproducibility across machines.
process {
    withName: QUARTO_PRERENDER {
        cpus = 1
    }
}
