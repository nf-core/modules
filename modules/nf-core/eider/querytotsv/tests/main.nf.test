
nextflow_process {

    name "Test Process EIDER_QUERYTOTSV"
    script "../main.nf"
    process "EIDER_QUERYTOTSV"

    tag "modules"
    tag "modules_nfcore"
    tag "eider"
    tag "eider/querytotsv"

    test("test-eider-querytotsv") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ], // meta map
				    [
                        file('${moduleDir}/tests/assets/template.sql', checkIfExists: true)
                    ]
				]

                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
