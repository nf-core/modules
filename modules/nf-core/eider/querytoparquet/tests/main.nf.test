
nextflow_process {

    name "Test Process EIDER_QUERYTOPARQUET"
    script "../main.nf"
    process "EIDER_QUERYTOPARQUET"

    tag "modules"
    tag "modules_nfcore"
    tag "eider"
    tag "eider/querytoparquet"

    test("test-eider-querytoparquet") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ], // meta map
				    [
                        file('${moduleDir}/tests/assets/template.sql', checkIfExists: true)
                    ]
				]

                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
