
nextflow_process {

    name "Test Process EIDER_QUERYTOPARTITIONEDPARQUET"
    script "../main.nf"
    process "EIDER_QUERYTOPARTITIONEDPARQUET"

    tag "modules"
    tag "modules_nfcore"
    tag "eider"
    tag "eider/querytopartitionedparquet"

    test("test-eider-querytopartitionedparquet") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ], // meta map
				    [
                        file('${moduleDir}/tests/assets/template.sql', checkIfExists: true)
                    ]
				]

                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
