process {
    withName: MALT_RUN {
        ext.args = {"${params.malt_run_args}"}
    }
    withName: MALT_BUILD {
        ext.args = {"${params.malt_build_args}"}
    }
    withName: MALTEXTRACT {
        ext.args = {"${params.maltextract_args}"}
    }
}
