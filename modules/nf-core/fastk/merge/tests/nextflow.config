process {

    withName: 'FASTK_ALLFILES' {
        ext.args   = '-t1 -p'
    }

    withName: 'FASTK_HISTONLY' {
        ext.args   = '-t1'
        ext.prefix = { "${meta.id}_${task.index}" }
        publishDir = [ enabled: false ]
    }

}
