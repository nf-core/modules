nextflow_process {

    name "Test Process FGUMI_SIMPLEXMETRICS"
    script "../main.nf"
    process "FGUMI_SIMPLEXMETRICS"

    tag "modules"
    tag "modules_nfcore"
    tag "fgumi"
    tag "fgumi/simplexmetrics"

    test("homo_sapiens - grouped bam") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ],
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/umi/test.paired_end.umi_grouped.bam', checkIfExists: true),
                    []
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out, unstableKeys: ["pdf"])).match() }
            )
        }

    }

    test("homo_sapiens - grouped bam - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ],
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/bam/umi/test.paired_end.umi_grouped.bam', checkIfExists: true),
                    []
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(sanitizeOutput(process.out)).match() }
            )
        }

    }

}
