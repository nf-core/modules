process {

    withName: RIBOTRICER_DETECTORFS {

        // NOTE: this is not a sensible value, but makes the module run for
        // testing purposes

        ext.args = '--phase_score_cutoff 0.05'
    }

}
