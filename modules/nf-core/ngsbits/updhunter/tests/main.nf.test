nextflow_process {

    name "Test Process NGSBITS_UPDHUNTER"
    script "../main.nf"
    config './nextflow.config'
    process "NGSBITS_UPDHUNTER"

    tag "modules"
    tag "modules_nfcore"
    tag "ngsbits"
    tag "ngsbits/updhunter"

    test("vcf") {
        when {
            params {
                module_args = "-c earlycasualcaiman -f hugelymodelbat -m slowlycivilbuck"
            }
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/vcf/ped/justhusky_minimal.vcf.gz', checkIfExists: true),
                    []
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }

    test("vcf - stub") {
        options "-stub"
        when {
            params {
                module_args = "-c earlycasualcaiman -f hugelymodelbat -m slowlycivilbuck"
            }
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/genome/vcf/ped/justhusky_minimal.vcf.gz', checkIfExists: true),
                    []
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
