{
    "id": "apptainer",
    "name": "Install Apptainer",
    "description": "Install Apptainer (Singularity)",
    "options": {
        "version": {
            "type": "string",
            "proposals": ["latest"],
            "default": "latest",
            "description": "Select or enter a version."
        }
    }
}
