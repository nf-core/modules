process {

    withName: MMSEQS_CREATEDB {
        ext.prefix = 'db'
    }

    withName: MMSEQS_CLUSTER {
        ext.prefix = 'cluster'
    }

    withName: MMSEQS_LINCLUST {
        ext.prefix = 'linclust'
    }

}
