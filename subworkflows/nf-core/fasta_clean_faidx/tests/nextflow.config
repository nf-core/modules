process {
    withName: 'SEQKIT_SEQ' {
        ext.args = '-u -v --only-id'
    }

    withName: 'SEQKIT_DOTS' {
        ext.args = "-p '\\.' -r '_'"
        ext.prefix = { "${meta.id}_dots" }
    }
}
