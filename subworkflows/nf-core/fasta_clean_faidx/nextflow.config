process {
    withName: '.*:FASTA_CLEAN_FAIDX:SEQKIT_SEQ' {
        ext.args = '-u -v'
    }

    withName: '.*:FASTA_CLEAN_FAIDX:SEQKIT_DOTS' {
        ext.args = "-p '\\.' -r '_'"
        ext.prefix = { "${meta.id}_dots" }
    }

    withName: '.*:FASTA_CLEAN_FAIDX:SEQKIT_TRIM' {
        ext.args   = "-p '(\\w+).*' -r '\$1'"
        ext.prefix = { "${meta.id}_trimmed" }
    }
}
