process {
    withName: 'XENIUMRANGER_IMPORT_SEGMENTATION' {
        ext.args = [
            '--units=pixel'
        ].join(' ').trim()

        cpus = 32
        memory = '120GB'
    }
    containerOptions = '-u $(id -u):$(id -g)'
}

docker.enabled = true
