# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/subworkflows/yaml-schema.json
name: "fastq_ngscheckmate"
description: Take a set of fastq files and run NGSCheckMate to determine whether samples
  match with each other, using a set of SNPs.
keywords:
  - ngscheckmate
  - qc
  - fastq
  - snp
components:
  - ngscheckmate/fastq
  - ngscheckmate/vaffastq
input:
  - ch_fastq:
      description: ""
  - ch_snp_pt:
      description: ""
output:
  - pdf:
      type: file
      description: A pdf containing a dendrogram showing how the samples match up
      pattern: "*.{pdf}"
  - corr_matrix:
      type: file
      description: A text file containing the correlation matrix between each sample
      pattern: "*corr_matrix.txt"
  - matched:
      type: file
      description: A txt file containing only the samples that match with each other
      pattern: "*matched.txt"
  - all:
      type: file
      description: A txt file containing all the sample comparisons, whether they match
        or not
      pattern: "*all.txt"
  - versions:
      type: file
      description: File containing software versions
      pattern: "versions.yml"
  - vaf:
      description: ""
authors:
  - "@SPPearce"
maintainers:
  - "@SPPearce"
