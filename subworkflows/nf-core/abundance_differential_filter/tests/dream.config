process {
    withName: 'VARIANCEPARTITION_DREAM' {
        ext.args = { [
            "--round_digits 2"
        ].join(' ').trim() }
        ext.prefix = { "${meta.id}_${meta.differential_method}" }
    }
}
