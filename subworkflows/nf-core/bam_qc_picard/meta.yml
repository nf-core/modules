name: "BAM_QC_PICARD"
description: Produces comprehensive statistics from BAM file
keywords:
  - statistics
  - counts
  - hs_metrics
  - wgs_metrics
  - multiple_metrics
  - bam
  - sam
  - cram
modules:
  - picard/collectmultiplemetrics
  - picard/collectwgsmetrics
  - picard/collecthsmetrics
input:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. [ id:'test', single_end:false ]
  - bam:
      type: file
      description: BAM/CRAM/SAM file
      pattern: "*.{bam,cram,sam}"
  - bai:
      type: file
      description: BAM/CRAM/SAM file index
      pattern: "*.{bai,crai,sai}"
  - meta2:
      type: map
      description: |
        Groovy Map containing reference information
        e.g. [ id:'genome' ]
  - fasta:
      type: optional file
      description: Reference fasta file
      pattern: "*.{fasta,fa,fna}"
  - meta2:
      type: map
      description: |
        Groovy Map containing reference information
        e.g. [ id:'genome' ]
  - fasta_fai:
      type: optional file
      description: Reference fasta file index
      pattern: "*.{fasta,fa,fna}.fai"
  - bait_intervals:
      type: optional file
      description: An interval list file that contains the locations of the baits used.
      pattern: "baits.interval_list"
  - target_intervals:
      type: optional file
      description: An interval list file that contains the locations of the targets.
      pattern: "targets.interval_list"
  - interval_list:
      type: optional file
      description: An interval list file that defines which contigs to include.
      pattern: "interval.interval_list"
output:
  - meta:
      type: map
      description: |
        Groovy Map containing sample information
        e.g. [ id:'test', single_end:false ]
  - coverage_metrics:
      type: file
      description: Alignment metrics files generated by picard CollectHsMetrics or CollectWgsMetrics
      pattern: "*_metrics.txt"
  - multiple_metrics:
      type: file
      description: Alignment metrics files generated by picard CollectMultipleMetrics
      pattern: "*_{metrics}"
  - multiple_metrics_pdf:
      type: file
      description: Pdf file containing the alignment metrics generated by picard CollectMultipleMetrics
      pattern: "*{pdf}"
  - versions:
      type: file
      description: File containing software versions
      pattern: "versions.yml"
authors:
  - "@matthdsm"
  - "@ramprasadn"
