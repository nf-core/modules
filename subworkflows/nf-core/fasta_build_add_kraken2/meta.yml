# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/subworkflows/yaml-schema.json
name: "fasta_build_add_kraken2"
description: KRAKEN2 build custom database subworkflow
keywords:
  - metagenomics
  - kraken2
  - database
  - build
  - custom
components:
  - kraken2/add
  - kraken2/build
input:
  - ch_fasta:
      type: file
      description: |
        Channel containing a meta with a list of FASTAs to be built
        Structure: [ val(meta), [ fasta1, fasta2, fasta3 ] ]
      pattern: "*.{fasta,fa,fna}"
  - ch_taxonomy_names:
      type: file
      description: |
        Channel containing a NCBI-style taxdump names file
        Structure: [ names.dmp ]
      pattern: "names.dmp"
  - ch_taxonomy_nodes:
      type: file
      description: |
        Channel containing a NCBI-style taxdump nodes file
        Structure: [ nodes.dmp ]
      pattern: "nodes.dmp"
  - ch_accession2taxid:
      type: file
      description: |
        Channel containing a NCBI-style taxdump accession2taxid (acc2tax) file
        Structure: [ accession2taxid_file ]
      pattern: "*.accession2taxid"
  - val_cleanintermediate:
      type: boolean
      description: |
        Boolean flag whether to clean up intermediate files after build or not
        Structure: [ val_cleanintermediate ]
      pattern: "true|false"
  - ch_custom_seqid2taxid:
      type: file
      description: |
        Optional custom or prebuilt seqid2taxid text file. If not provided, it will be generated by kraken2-build.
      pattern: "seqid2taxid.map"
output:
  - db:
      type: directory
      description: |
        Channel containing KRAKEN2 database directory files.
        Use `ext.prefix` in a modules.conf file to change default name
        Structure: [ val(meta), path(db) ]
      pattern: "*/"
  - versions:
      type: file
      description: |
        File containing software versions
        Structure: [ path(versions.yml) ]
      pattern: "versions.yml"
authors:
  - "@jfy133"
maintainers:
  - "@jfy133"
