// IMPORTANT: This config file should be included to ensure that the subworkflow works properly.
process {
    withName: '.*VCF_FILTER_BCFTOOLS_FILTERVEP:BCFTOOLS_VIEW' {
        ext.args   = { [
            '--output-type z',
            '--write-index=tbi',
        ].join(" ") }
    }
}
