process {
    withName: 'ENSEMBLVEP_FILTERVEP' {
        ext.args   = { "--filter \"HGNC_ID in ${feature_file}\"" }
    }

    withName: 'BCFTOOLS_VIEW' {
        ext.prefix = { "${meta.id}_filtered" }
        ext.args = '-e "swegenAF>0.1" --no-version --output-type z --write-index=tbi'
    }
}
