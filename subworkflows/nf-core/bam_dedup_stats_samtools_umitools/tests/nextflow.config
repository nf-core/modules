env.PYTHONHASHSEED = '0'

process {
    withName: 'UMITOOLS_DEDUP' {
        ext.args = '--random-seed=100'
    }

    withName: 'SAMTOOLS_VIEW_PRIMARY' {
        ext.args   = '-F 0x900 -b'
        ext.prefix = { "${meta.id}.primary" }
    }
}
