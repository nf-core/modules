// IMPORTANT: Add the ext.prefix configuration to your modules.config
// Avoid input/output filename collisions when minimap2/align receives uBAM input.

process {
    withName: MINIMAP2_ALIGN {
        ext.args = '-x map-ont -Y --MD'
        ext.prefix = { "${meta.id}.aligned" }
    }
}
