params {
    withName: 'SE_GENE_UNIFIED' {
        ext.args = '--assay_names counts_gene,counts_gene_length_scaled,counts_gene_scaled,lengths_gene,tpm_gene'
    }

    withName: 'SE_TRANSCRIPT_UNIFIED' {
        ext.args = '--assay_names counts,lengths,tpm'
    }
}
