// IMPORTANT: This config file should be included to ensure that the subworkflow works properly.
process {
    withName: '.*VCF_VEMBRANE:VEMBRANE_SORT_DEFAULT' {
        ext.prefix = { "${meta.id}.sorted" }
    }
}
