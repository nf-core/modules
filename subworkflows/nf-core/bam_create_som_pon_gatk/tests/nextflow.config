process {
    withName: GATK4_MUTECT2 {
        ext.prefix = { meta.num_intervals <= 1 ? "${meta.id}.mutect2" : "${meta.id}.mutect2.${intervals.baseName}" }
        ext.args = "--max-mnp-distance 0"
    }
    withName: GATK4_SPLITINTERVALS {
        ext.args = "--scatter-count 3"
    }
}
